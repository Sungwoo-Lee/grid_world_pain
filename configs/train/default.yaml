# Default Training Configuration
training:
  episodes: 100
  num_envs: 128
  checkpoint_frequency: 2000 # Save model every N episodes
  video_during_training: true
  seed: 42
  device: "auto"
episodes: 100
seed: 42
tag: "default"
