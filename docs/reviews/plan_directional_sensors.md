---
title: "Plan review — directional sensors (per-cell olfaction + anisotropic visual PSF)"
topic: sensors
status: active
created: 2026-08-19
last_updated: 2026-08-20
---

# Plan review — DIRECTIONAL_SENSORS_PLAN

**Object**: [[DIRECTIONAL_SENSORS_PLAN]] (`docs/develop/active/sensors/DIRECTIONAL_SENSORS_PLAN.md`)
**Reviewed by**: plan-reviewer
**Date**: 2026-08-19

## Verdict

**NOT READY** — one Critical finding in the visual-mask semantics; the engineering core (kernel, olfactory diamond, parity tests, cost claims) is sound and well-evidenced.

Plain-language summary: the plan adds two opt-in sensor upgrades — smell sampled on a small diamond of points so it carries a direction, and vision blurred along the line of sight so distance becomes vague while bearing stays sharp — with everything off by default and byte-identical observations promised for the shipped config. Most of it holds up: the kernel math matches the study's working prototype, the "blur costs nothing" claim is backed by a real measurement, and the parity claims survived my own bitwise micro-checks. The blocker is the new per-entity invisibility option. The plan justifies its masking design with "'hidden' should mean no leakage", but the mechanism it specifies does leak: a `far`-masked ("hidden") entity still deposits a deterministic signal into the cell the agent stands on — about 13.5% of a visible adjacent entity's peak signal when the hidden entity is one cell away, fading only as 1/d². A recurrent agent can learn to detect a "hidden" predator from that leak, which would silently invalidate any future experiment built on the premise that the agent cannot see it — the wrong conclusion would look exactly like the interesting result ("anticipatory avoidance of an unseen threat"). The plan's own test suite would cement the leak as specified behaviour. The fix is cheap now: gate on the *entity's* distance (zero its whole column) or re-specify what `far` means and have the user sign off.

Severity legend: 🔴 Critical = fix before going further · 🟡 Moderate = likely costs a re-run · 🟢 Low = cosmetic · ❓ Open = an assumption nobody has verified yet.

## Findings

| # | Sev | Location | Issue | Suggested fix |
|---|---|---|---|---|
| 1 | 🔴 | Plan §Decisions "Mask ordering" + §(b) `_visual_mask_gate` | The `far` gate zeroes **cells** at Manhattan distance ≥ 1, not the **entity** — so with blur on, a `far`-masked entity at any distance d still writes `exp(−d²/2σ_∥²)/(2πσ_∥σ_⊥)` into the centre cell. At the shipped knobs (scale 0.5, ρ 3, floor 0.5) that is w = 0.086 at d=1, 0.043 at d=2, 0.029 at d=3 — 13.5%/6.8%/4.5% of an adjacent visible entity's own-cell peak (0.637). Deterministic, noise-free by default, integrable by the recurrent policy: "hidden" entities are detectable. This directly contradicts the row's own rationale ("'hidden' should mean no leakage"), and `test_visual_mask.py` ("far … leaves the centre") would enshrine it. The study left exactly this Open (VISUAL_PSF_MECHANISM_STUDY §Decisions/Open "Mask order"); the plan resolved it to the leaking option while citing leak-freedom as the reason. (`all` is fine — column zeroed everywhere.) | Either (a) gate `far` on the **entity's** distance — zero the entity's entire `[C]` column when its distance ≥ 1, giving true contact-only visibility under blur and unchanged behaviour without blur — or (b) keep per-cell gating but re-specify `far` as "peripheral blindness with a residual centre-cell signal", quantify the leak in the config comment, and get explicit user sign-off, since `visual_mask` is a user-requested feature whose semantics under blur was never put to the user. |
| 2 | 🟡 | Plan §File Changes `config_loader.py` (five `get_mandatory` keys) vs `evaluation.py:235-256, 355-356`, `scripts/verification/check_olfaction_parity.py`, `tests/env/test_backward_compat_configs.py` | Unstated side effect: runs dump their **fully merged** config to `models/config.yaml` (`train.py:881-883`, `dreamer_srl_main.py:1040-1043`), and every re-evaluation tool loads that snapshot straight into `load_env_params` with no default-merge underneath. Old snapshots lack the five new keys → **every pre-change run becomes un-evaluatable / un-re-renderable / un-resumable** (`ValueError: 'sensory.olfactory_sensor_range' is required but missing`) until backfilled. Additionally: `check_olfaction_parity.py` + its two `configs/verification/*.yaml` crash (they load standalone; verified empirically that standalone configs missing a mandatory key raise); and ~98 standalone configs under `configs/environment`+`configs/verification` get silently demoted to "stale-skip" in `test_backward_compat_configs.py:66-90`. Training itself is safe (`get_default_config()` merges the updated default underneath). Precedent exists — the interoceptive keys did the same and 48 configs were swept — but this plan's File Changes touch only `default.yaml`, so the sweep is missing and the cost is undisclosed. | State the consequence in the plan and let the user own it. Minimum: add the new keys to the live (non-archive) standalone configs incl. `configs/verification/olfaction_parity_*.yaml`; decide explicitly whether historical `models/config.yaml` snapshots get a backfill note (a documented one-liner `yq` edit) or are accepted as broken. Gate the three blur knobs' `get_mandatory` behind `visual_blur_enabled` per CONFIG_GUIDE.md:211's conditional-key pattern to shrink the blast radius. |
| 3 | 🟡 | Plan §File Changes train.py/dreamer + §Test Plan `test_modality_fingerprint.py` | Misdiagnosis: the plan claims two stages differing only in `olfactory_sensor_range` would "pass validation, loading weights shaped for the wrong observation". False — both validators check `obs_dim` equality *before* the fingerprint (`train.py:816-822`, `dreamer_srl_main.py:806-812`), and an olfactory-range change always changes obs_dim when the other 13 fields are equal; it is caught today. Consequently the proposed test ("two configs differing only in `olfactory_sensor_range` are rejected") **passes without the fingerprint change** — it exercises the pre-existing obs_dim check, i.e. it cannot fail for the reason it exists (circular-verification trap). The genuinely new protection is `visual_blur_enabled` (semantics change at identical dim) — and the test plan never tests that case. Adding both fields is still right (and closes a real compensated-swap corner, see ❓c), but "the highest-risk change in this plan" is actually its safest. | Rewrite the test: (a) a pair differing only in `visual_blur_enabled` (same obs_dim) must be rejected **with the fingerprint error message**, in both entry points; (b) if the olfactory-range pair is kept, assert which error fired. Correct the plan's motivating paragraph. |
| 4 | 🟡 | Plan §Checkpoints CP6 vs `visual_psf_study/bench_aniso.py:22-80` | CP6 ("re-run `bench_aniso.py` after implementation") is circular: the benchmark defines its **own embedded copy** of the kernel (`sense_visual_aniso`, local function) and compares it against `sense_visual` imported from src. Re-running it after implementation re-measures the prototype, not the implemented code — it reproduces the study's numbers no matter what was built. | Point CP6 at the real thing: benchmark `sense_visual` with `visual_blur_enabled: true` vs `false` through `env.step` (the bench's timing harness can be reused; only the measured callable changes). |
| 5 | 🟡 | Plan §(a) docstring, §(b) intro sentence, §Test Plan | Both "parity by construction" claims are overstated as written. (b) says "the exact-match path stays untouched, so `visual_blur_enabled: false` is byte-parity by construction" — but the very snippet beneath it changes that path twice: the activity mask moves from `all_props` (sensor.py:230) onto `W`, and a new `[C,E]` gate multiply is added to **both** branches. (a)'s claim that vmap-over-one-cell "reproduces the pre-v3.1 single sample exactly" changes the compiled program (batched reduction). Both are *probably* bit-identical — my CPU micro-checks confirm bitwise equality for both transformations, including negative property values — and the plan does mandate the tests (good), but only the visual risk row has a fallback; olfaction has none if GPU/XLA fusion breaks parity by an ulp. Fixture portability is also unstated: a reference dumped on one backend can differ bitwise on another. | Drop the "by construction" language (CP1/the tests are the actual guarantee). Add the olfaction fallback: static-branch to the current three-call path when `olfactory_sensor_range == 0`. Pin the parity fixtures' backend (generate and compare on CPU, like the existing `test_visual_parity.py` net). |
| 6 | 🟡 | Plan §Docs vs CLAUDE.md "Config system" | Maintenance contract short one doc: a config-schema change must update **CONFIG_GUIDE.md** *and* `02_config_schema.md` in the same change. The plan lists `02_config_schema.md` and CONFIG_CRITICAL_SETTINGS but omits CONFIG_GUIDE.md (whose §"Adding a new key" checklist at CONFIG_GUIDE.md:208-211 is also the atomic-landing recipe this change must follow). | Add CONFIG_GUIDE.md to the Docs list. |
| 7 | 🟢 | Plan §File Changes headings | Stale refs: the fingerprint lives at `train.py:784`, not "~line 485" (the ~485 figure is the dreamer file's stale "Ported verbatim from train.py:L485-L503" comment propagating). And "13-field" wording appears **only** in `dreamer_srl_main.py:771,775` — `train.py`'s docstring never says "13" and `tests/env/test_no_recompile.py` contains no such wording (its docstring is about animal-class-ordering recompiles). Harmless, but two of the three claimed edit sites don't exist. | Fix the line refs; scope the wording edit to the dreamer file. |

## The three unilateral decisions (audit requested by the caller)

| Decision | Verdict | Basis |
|---|---|---|
| Normalisation by full analytic mass | **Supported.** | Fig 4's measured numbers (over-diamond normalisation removes distance falloff entirely: 0.513/0.698/0.486 at d=1/2/4; full mass gives 0.262/0.161/0.032). The alternative silently deletes the feature's point. Bonus the plan could state: full-mass normalisation gives vision an intrinsic ~1/d² fade, so no separate decay term is needed. |
| One width + anisotropy ratio ρ | **Supported — and equivalent to the study's own preference.** | σ_⊥ = (scale/ρ)·d is linear in d, i.e. exactly the study's "fixed angular blur" (Δθ = arcsin(scale/ρ)) under different knob names, with ρ=1 giving the isotropic ablation as a single config value. The floor breaking proportionality at short range is documented as intended. No overreach here. |
| Mask after blur (per-cell gate) | **Not supported — the rationale is inverted.** See finding 1. | The study explicitly left this Open; the chosen option leaks into the centre cell while the row's justification claims no leakage. |

One caveat the plan dropped: the study marks **sensor range as "Blocking — every question above resolves differently at range 1 than at range 3"**. Deferring the *values* to experiment configs is legitimate for an implementation plan, but the plan should say that the shipped defaults (scale 0.5, ρ 3, floor 0.5) are placeholders whose sanity was studied at specific ranges, not blessed training values.

## Open assumptions (❓)

- **a. Out-of-bounds diamond cells.** `sense_olfaction_cells` applies no in-bounds mask: an agent at a wall samples the chemical field at points *outside the world*. Physically coherent (the field is defined by distances) but undecided — the visual sensor zeroes OOB cells, collision marks them; olfaction would be the only sense that reads through walls. Nobody chose this.
- **b. The on-source decay-2.0 rule.** `sense_resource` returns decay 2.0 when the sampling point sits on a source (`sensor.py:14`). Today only the agent's own cell can trigger it; with per-cell sampling any diamond cell can, so it fires routinely. OLFACTORY_EXPANSION_STUDY lists this as **Open ("Keep, or make it continuous?")**; the plan silently resolves it to "keep" by reusing `sense_resource` unchanged, without listing it in the decision table.
- **c. Fingerprint coverage is inconsistent.** The three continuous blur knobs are deliberately traced (good, CP5) but therefore absent from the fingerprint — two curriculum stages differing in ρ or scale (a large same-dim semantics change) pass validation, while a `visual_blur_enabled` flip is rejected. Also pre-existing and adjacent: `visual_vector_size` is not fingerprinted, so a compensated swap (e.g. olfactory range +20 dims vs visual V 8→28 at range 0) passes both checks today; the same edit could close it.
- **d. Blur-curriculum foreclosure.** Fingerprinting `visual_blur_enabled` forbids a "train sharp → continue blurred" curriculum by construction. That is the check's documented purpose (no semantic swaps across stages), but it is a capability being removed before anyone asked for it — worth one line to the user.

## Passes run

1 (verifiability incl. circular-verification: findings 3, 4, 5), 2 (project rules: finding 6; no-fallback-defaults honoured — finding 2 is about its blast radius, not a violation; survival-step rule N/A, no metric claims; conda path correct throughout), 3 (side effects: finding 2; the bundled `build_sensory_viz` labels-bug fix is verified real — `draw_spectrum_pod` at `renderer_v2.py:140` takes no labels while the diamond path `renderer_v2.py:509-518` consumes them — and justified), 4 (assumptions/ordering: ❓ list; single `EnvParams` construction site at `config_loader.py:1356`, so the state.py edit is contained), 5 (prior art: KNOWN_BUGS grep — the fixed noise-channel-order and extends-dropped bugs are relevant history, no collision; llm_wiki `env_entities`/`config_system` indexes — no prior plan on this; the `decay_power` 1.0-vs-2.0 drift insight corroborates the plan's doc-staleness repair). Skipped: 6 (experiment-plan specifics — this is an engineering plan; obs↔noise sync was checked anyway: `apply_perceptual_noise` genuinely follows the breakdown automatically, name-keyed at `sensor.py:252-255`, per-dim arrays sized from the dict — the plan's "follows automatically, but verify" claim is honest and correct) and 7 (analysis-verdict pass — no verdict under review).

## Cost of being wrong

If finding 1 ships as specified, the first masking experiment produces a paper-level wrong conclusion that is indistinguishable from the hoped-for result (an agent "anticipating" a hidden predator it can actually faintly see) — that is the expensive one, and it is two lines of spec to fix now. Finding 2 costs a confused day and an unplanned config sweep the first time anyone re-evaluates a pre-change run — loud, recoverable, but currently undisclosed. Everything else is caught at test time and costs minutes.

## Exit conditions (what flips the verdict)

1. Resolve `far` semantics: entity-distance gating, or re-specified leak semantics with explicit user sign-off (finding 1).
2. Disclose and decide the old-config/old-run consequence, and add the standalone-config sweep to File Changes (finding 2).
3. Re-point `test_modality_fingerprint.py` at the case only the fingerprint catches, and CP6 at the implemented code (findings 3, 4).

Findings 5–7 and the ❓ items are author's-discretion improvements; they do not gate the verdict.

**Owners**: finding 1 → user decision, then `senior-developer` (plan revision); findings 2–7 → `senior-developer`; ❓a/❓b → user (design calls the studies left open); no `bug-curator` items — nothing here contradicts the Known Bugs registry, and no new latent bug in existing code was found beyond what the plan already flags.

---

# RE-REVIEW — 2026-08-20

**Object**: the revised [[DIRECTIONAL_SENSORS_PLAN]], after the author's response to the first round, plus the material that landed since: [[ONSOURCE_RULE_STUDY]] (resolving the on-source decay rule), two new benchmarks (`visual_psf_study/bench_visual_variants.py`, `olfactory_expansion_study/bench_olfaction.py`), a reversal of implementation note 1 (the `[C,E,2]` guidance), and answers to all four previously-open user decisions.
**Reviewed by**: plan-reviewer (second pass)

## Verdict

**SOUND WITH CONCERNS** — every first-round exit condition is met and verified against the code, not the author's account. The Critical leak is genuinely fixed in the spec and its regression test is real. The new material is largely solid: I independently reproduced the study's two headline technical claims (the float32 bit-identity of the on-source rewrite, and the GPU reduced-precision behaviour behind the note-1 reversal) on this machine. What keeps the verdict at "with concerns" is one undisclosed blast radius and three spec-drift hazards, all cheap to fix before implementation: the "parity-preserving" on-source change **silently alters observations for eleven live configs that ship `decay_power: 2.0`** — including the five stage configs of an already-analyzed continual-learning experiment — and the plan's code snippets have not all caught up with its own corrected prose (the mask-gate call site still shows the leaking form; the olfaction snippet lacks the out-of-bounds zeroing the user just decided on, and no test covers it).

Plain-language summary: the revised plan holds up. Smell-on-a-diamond and direction-stretched visual blur are specified implementably, everything stays off by default, and the promise that the shipped configuration produces byte-identical observations is now backed by real measurements — including a GPU bit-parity measurement that closes the gap my first round could only check on CPU. The one thing nobody has said out loud: the small "make the on-the-source smell constant principled" rewrite is only a no-op for configs where smell falls off linearly with distance (today's default). Eleven live configs — and essentially every historical run from before the default changed — use the old steeper falloff, and for them the rewrite doubles the smell reading whenever the agent stands on a smelly thing. That is by design (the study argues the new value is the *correct* one), but the study presented the affected regime as hypothetical when it is in fact the project's own history, so the user signed off on incomplete information. It needs a disclosure and a change-log entry naming the affected configs, not a redesign.

Severity legend: 🔴 Critical = fix before going further · 🟡 Moderate = likely costs a re-run · 🟢 Low = cosmetic · ❓ Open = an assumption nobody has verified yet.

## Half 1 — do the fixes fix? (all seven verified against code, not the author's account)

| R1 finding | Verified state |
|---|---|
| 🔴 1 `far`-mask leak | **Fixed in spec.** The gate now zeroes the *entity's* whole `[C]` column when its Manhattan distance ≥ 1 (plan §(b) `_visual_mask_gate`), which eliminates the centre-cell leak exactly — a 0.0 gate multiplied into finite blur weights (finite because of `sigma_floor`) is exactly 0.0. Agent standing on a `far`-masked entity: `d_e = 0` → column kept → visible, as the semantics say. The rewritten `test_visual_mask.py` is a **real regression test**: "exactly zero to every cell including the centre" fails against the old per-cell gate (which leaves 0.086 in the centre at d=1), so it cannot enshrine the leak. Residual: finding N2 below — the plan's call-site snippet contradicts its own definition. |
| 🟡 2 mandatory-key blast radius | **Section is accurate.** Verified `evaluation.py:130-142` (`_load_eval_config`): loads the run's `models/config.yaml` snapshot and merges only `configs/evaluation/default.yaml` + `configs/visualization/default.yaml` — never the environment default — so pre-change snapshots do raise. Stale-skip demotion confirmed at `tests/env/test_backward_compat_configs.py:74-90` ("is required but missing" → `pytest.skip`). The ~98-config sweep, the verification-script update, and the snapshot remedy are all in File Changes. One line ref drifted: the config dump is at `train.py:896-898`, not 881-883. |
| 🟡 3 fingerprint misdiagnosis | **Corrected correctly.** Verified `train.py:816-827` and `dreamer_srl_main.py:800-812`: obs_dim equality is checked before the fingerprint in both, so an olfactory-range change is already rejected today. The retargeted test (same obs_dim, differing `visual_blur_enabled`, must assert the *fingerprint* error in both entry points) is **non-vacuous**: without the new field that pair passes both checks. The "13-field" wording indeed lives only at `dreamer_srl_main.py:772,776`. |
| 🟡 4 CP6 circular | **Fixed.** CP6 now requires repointing the benchmark at the real `sense_visual` with `visual_blur_enabled: true`. |
| 🟡 5 parity overstated | **Fixed, and strengthened beyond the ask.** "By construction" language is gone; olfaction gains the static range-0 fallback; fixtures pin the CPU backend. Additionally [[VISUAL_PSF_MECHANISM_STUDY]] §Implementation variants now reports the restructured OFF path (mask on `W` + gate multiply) measured **bit-identical on GPU at ranges 0/1/2** against the real `sense_visual` — closing the CPU-only gap my first round flagged. |
| 🟡 6 CONFIG_GUIDE.md | **Fixed.** Listed in Docs; the atomic-landing recipe is where cited (`CONFIG_GUIDE.md` §5, ~lines 205-215). |
| 🟢 7 line refs | **Fixed.** The two phantom edit sites are gone; the surviving refs check out to within a few lines (`sense_visual` def at `sensor.py:146`; breakdown olfaction at 371-372; `build_sensory_viz` at 392; `EnvParams` at `state.py:257-260`). |

## Half 2 — the material this review had not seen

**ONSOURCE_RULE_STUDY / option B — the bit-identity claim is true; I verified it myself, three ways.** In float32: numpy (`1.0/np.power(0.5, 1.0)` bit-equals `2.0`, both `0x40000000`), the traced expression under `jax.jit` on **CPU**, and under `jax.jit` on **GPU** (this node's RTX 4090s), including embedded in the full `jnp.where(dist < 0.001, ...)` line over a distance vector — new and old code paths byte-equal at γ=1. The study's "unreachable discontinuity" argument is also correct: integer entity and cell coordinates make every nonzero distance ≥ 1, so the 0.001–1 region genuinely cannot be sampled. The problem is not the math — it is finding N1 below.

**bench_visual_variants.py** is the right kind of benchmark: its OFF-path parity check compares against the *imported* `sense_visual` (not an embedded copy, unlike the old `bench_aniso.py`), and its precision experiment flips `jax_default_matmul_precision` live. One structural caveat: **after** implementation, `sense_visual`'s OFF path *becomes* the bench's `off_restructured`, so re-running the bench compares new-vs-new — CP0's "re-confirm after implementation" wording inherits a mild version of the CP6 circularity (finding N5). The pre-change ground truth then lives only in CP1's stored arrays and the stored-reference parity tests, which is fine — CP0 just should not claim otherwise.

**The note-1 reversal is sound, and I reproduced its mechanism independently.** On this RTX 4090, the `[C,2] @ [2,E]` projection matmul runs in reduced precision by default while the same contraction over a materialised `[C,E,2]` tensor (einsum or elementwise multiply-sum) is full float32 — max error vs a float64 reference 1.9e-06, identical to the elementwise form, versus the study's measured 5.4e-04 for the matmul form. Not over-generalised in the conclusion: on pre-Ampere GPUs (the lab's 2080 Ti nodes have no TF32) the matmul form is full-precision anyway, so "use V2" is safe on every card in the cluster, and the measured speed spread (µs at ~1.5% of `env.step`) makes the choice cost-free. The implementer's instruction is unambiguous — V2's exact code is in the bench. Two nits: the plan's "5.4e-04 against 3.6e-07" is V1-vs-V2 at two precision settings, not V2's error; and whether XLA keeps einsum out of the reduced-precision path is an implementation detail per XLA version — the mandated precision pin in `test_visual_psf_kernel.py` is the durable guarantee, and it is mandated. Note also the plan's own blur path still ends in `W @ all_props`, which *does* run TF32 on Ampere+ — harmless for the science (σ=0.2 noise dwarfs it, and the study says so for the analogous pre-existing case), but the "fatal for a geometry unit test" rationale applies only to the kernel tests, which test `W`, not the matmul output.

**Consumers checked (none break):** `evaluation_core._sensor_stat_columns` names olfaction columns generically (`olf_{i}` over the reported dim) and hard-fails on unknown modalities — expanded olfaction flows through (`src/utils/evaluation_core.py:158-196`). The Dreamer SRL hierarchical encoder consumes `get_observation_breakdown` widths dynamically (`src/algorithms/dreamer_srl/agent.py:1411-1425`) — a wider olfaction branch just gets wider. The noise system is name-keyed off the same dict (verified round 1). The renderer's diamond path consumes labels (verified round 1), which is exactly why the plan's bundled labels-bug fix in `build_sensory_viz` is needed. `check_olfaction_parity.py` compares neutral-vs-pred at distances ≥ 1 and `check_observability_gates.py` asserts only Injury/Nutrition slots — neither breaks under option B, so the plan's "no existing test changes" is literally true (which is part of why finding N1 is invisible).

**The four answered decisions:** ranges (2/1) — arithmetic checks out (27−8+104 = 123; 27−5+25 = 47; combined 143) against `default.yaml`'s range-0/V=8/5-dim layout. Decision 3 (knobs unfingerprinted, deliberate comment) and 4 (blur-curriculum foreclosure accepted) are implementable as written. Decision 2 (option B) → finding N1. Decision 1 (zero OOB olfactory cells) → finding N3: implementable, consequence honestly recorded, but not actually folded into the plan's code snippet or test plan.

## New findings

| # | Sev | Location | Issue | Suggested fix |
|---|---|---|---|---|
| N1 | 🟡 | Plan §on-source rule + [[ONSOURCE_RULE_STUDY]] §Options vs `configs/verification/olfaction_parity_{neutral,predator}.yaml:55,62`, `configs/verification/observability_gates_S{1-4}.yaml:267`, `configs/continual/nmn_double_return_stages/0{1-5}_*.yaml` | The option-B sign-off rests on a false framing. Study and plan present γ ≠ 1 as hypothetical ("appears only when γ is changed, which OLFACTORY_EXPANSION_STUDY *contemplates*") — but γ=2.0 was the project's **historical default** (the hv-probe configs say so verbatim: "hg10 trained with decay_power=1, NOT the default 2.0") and ships today in **11 live non-archive configs**, including all five stages of [[NMN_CONTINUAL_DOUBLE_RETURN_PROBE]] — an **ANALYZED** experiment with a headline claim (~107-132 survival-step win) and a pre-registered follow-up replication. For every γ=2 config, option B changes the on-source olfactory reading from `2.0·prop` to `4.0·prop` — on ~⅓ of steps by the study's own frequency measurement. Failure scenario: the multi-seed replication the probe doc calls for launches post-change and is compared against Round 2 numbers — the two vintages differ in observation semantics, and nothing flags it (no test touches d=0 at γ=2; re-evaluations of old checkpoints load the run's own γ=2 snapshot into the *new* code). The plan's "no existing test changes" is true; "parity holds" is true only of `default.yaml`. | Disclose, don't redesign: (a) name the 11 live configs (and "every pre-dp1 historical snapshot") in the plan's on-source section; (b) the [[CONFIG_CRITICAL_SETTINGS]] change-log entry must state "observations change for any config with `decay_power ≠ 1.0`, on-source readings scale by `2^(γ−1)`"; (c) one line in the NMN probe doc: post-change reruns/re-evals are not observation-identical to Round 2. If cross-vintage comparability there matters more than curve-correctness, the fallback is option A (keep 2.0) — parity everywhere, trap documented instead of fixed; that trade is the user's, made on the *correct* premise this time. |
| N2 | 🟡 | Plan §(b): call site `W = W * _visual_mask_gate(offsets, all_mask)` vs the definition `_visual_mask_gate(agent_pos, all_pos, all_mask)` | The Critical fix's own snippet pair disagrees. The call site still passes `offsets` — the first draft's *per-cell* form, from which entity distance cannot even be computed; the definition below it implements the corrected entity-distance gate. An implementer transcribing the `sense_visual` snippet reproduces the leak the fix exists to kill. `test_visual_mask.py` as specified would catch it (exact-zero-at-centre fails against the per-cell gate), so this costs a debugging loop, not a shipped leak — but a plan should not carry both versions of its most important line. | Change the call site to `_visual_mask_gate(agent_pos, all_pos, all_mask)`. One line. |
| N3 | 🟡 | Plan §(a) snippet + §Test Plan vs answered decision 1 | The user's OOB decision is recorded but not folded in. The `sense_olfaction_cells` snippet contains **no** in-bounds mask (it predates the answer), the instruction lives only in prose (spec detail 4), and **no test in the Test Plan asserts OOB zeroing** — `test_olfaction_diamond.py` checks bearing ordering only. An implementer following the snippet ships read-through-walls olfaction with a fully green suite, and the plan's carefully-recorded "wall cue" caveat for future behaviour analysts becomes a description of a cue that does not exist. Also a wording trap: "the same `is_in_bounds` mask `sense_visual` already builds" cannot be literally reused — vision's mask is over the *visual* diamond; olfaction needs one rebuilt from the *olfactory* offsets whenever the two ranges differ. | Add the mask to the snippet (rebuild from olfactory offsets), and add one assertion to `test_olfaction_diamond.py`: agent in a corner at range 1 → the two OOB cells' V channels are exactly zero, the in-bounds ones are not. |
| N4 | 🟡 | Plan §EnvParams (`res_visual_mask`/`animal_visual_mask`/`obs_visual_mask` as traced arrays) vs the fingerprint discussion | Per-entity `visual_mask` is a new **same-dim semantics knob that the fingerprint discussion never mentions**. Two curriculum stages identical except `visual_mask: none → all` on the predator pass obs_dim *and* fingerprint validation, silently blinding the agent mid-run — exactly the class `visual_blur_enabled` was fingerprinted for, and sharper than the accepted-unfingerprinted continuous knobs (it is discrete, entity-targeted, and the natural lever of a future masking experiment). Decision 3's acceptance covers "ρ, radial scale and σ floor" only. Trap within the trap: naively appending the jnp arrays to the fingerprint tuple breaks it — tuple equality on arrays returns arrays, and the `fp_i != stage0_fingerprint` comparison then raises (or worse, mis-evaluates); they must go in as e.g. `tuple(map(int, ...))`. | Either add the mask arrays to both fingerprints as int tuples, or extend decision 3's deliberate at-the-fingerprint-site comment to name `visual_mask` as knowingly unfingerprinted. Say which in the plan. |
| N5 | 🟢 | Plan CP0 | "Re-confirm after implementation with `bench_visual_variants.py`" quietly changes meaning after implementation: once `sense_visual`'s OFF path *is* the restructure, the bench compares the implementation against its own prototype — the pre-change reference no longer exists in it. Parity-with-today is then carried entirely by CP1's stored arrays and `test_visual_blur_disabled_parity.py`'s stored reference, which is sufficient — CP0 should just say that instead of implying the bench still measures "today". | Reword CP0: post-implementation, the bench re-run checks implementation-matches-prototype; parity-with-today is CP1's stored fixture. |
| N6 | 🟢 | Plan §on-source parity test; note 1 wording; `train.py:896-898`; plan frontmatter | Batch of nits: (a) the specified parity assertion (`1/(0.5**1.0) == 2.0` in float32) tests Python semantics, not the code path — assert the **traced** `jnp` expression under `jit` instead (I verified it holds on CPU and GPU, so the stronger test passes today); (b) "5.4e-04 against 3.6e-07" conflates V1-vs-V2 at two precision settings with V2's error; (c) config dump is `train.py:896-898`, not 881-883; (d) plan frontmatter `last_updated: 2026-08-19` predates its own 2026-08-20 update. | As listed; all one-liners. |

## Open assumptions (❓)

- **e. XLA keeps the einsum contraction out of the reduced-precision path.** True on this 4090 and this jaxlib (measured, twice — the study's and mine); not a contract. The pinned-precision kernel test is the durable net; if a jaxlib upgrade ever flips it, the test catches it. Nothing to do now.
- **f. The frequency numbers (31.6%/73.4%/91.2%) were not re-measured here.** They gate nothing in the plan (they motivate caring about the rule, not any parity claim), so I audited the script's existence, not its output.

## Passes run

1 (verifiability: N2, N3, N5 — and the two headline empirical claims re-verified from scratch rather than trusted), 2 (project rules: `get_mandatory` kept with disclosed blast radius ✓; all three maintenance-contract docs now listed ✓; conda interpreter used for all verification runs ✓; frontmatter nit in N6), 3 (side effects: N1 — the one change in this plan that alters live-config behaviour while being sold as a no-op; N4), 4 (assumptions/ordering: all four answered decisions audited for implementability; decision 1 → N3, decision 2 → N1), 5 (prior art: KNOWN_BUGS grep on olfaction/decay/precision — only the fixed noise-modality-name row, no collision; no prior doc contradicted). Skipped: 6 (engineering plan, not an experiment design — obs↔noise sync re-confirmed anyway via the breakdown-keyed noise path) and 7 (no analysis verdict under review — though N1's scenario is precisely how a future analysis verdict would go wrong).

## Cost of being wrong

If N1 ships undisclosed, the cost lands months later as a cross-vintage comparison in the NMN continual-learning line that silently compares two different sensory worlds — a confused day at best, a wrong replication verdict at worst; the disclosure costs three sentences now. N2 costs one implementer debugging loop (the test catches it). N3 is the quiet one: no test catches it, so read-through-walls olfaction could ship green and make the recorded wall-cue caveat false — found only when someone visualises the diamond at a wall. N4 costs nothing until the first masking curriculum, where it becomes a silent mid-run semantics change of exactly the kind two validators exist to prevent.

## Exit condition

None of N1–N6 gates implementation the way the round-1 Critical did; they gate *sign-off honesty and spec convergence*. The verdict reaches plain SOUND when N1's disclosure lands (plan text + CONFIG_CRITICAL_SETTINGS change-log wording + one line in the probe doc, or an explicit user re-confirmation of option B on the corrected premise) and the three snippet/test drifts (N2, N3, N4) are folded into the plan text. All are text edits to one plan and two docs; none require re-study.

**Owners**: N1 → user (re-confirmation on corrected premise), then `senior-developer` (disclosure text); N2, N3, N5, N6 → `senior-developer`; N4 → user chooses fingerprint-vs-comment, `senior-developer` records it. No `bug-curator` items: nothing found contradicts the Known Bugs registry, and the latent hazards found live in the plan, not in existing code.

Reviewed by: plan-reviewer
