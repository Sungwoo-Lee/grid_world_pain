# Diary — 2026-08-25

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 14:43 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 14:43 | 4efbe660 | implemented | Episode-level factor analysis: what makes the agent hide (1M episodes, a01) | [hiding_drivers](../../scripts/analysis/hiding_drivers.py) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 15:30 — bug-curator: recorded off-by-one episode-boundary guard in the felt-pain reconstruction used by the hiding-drivers analysis (FIXED, scripts/analysis/supplementary/noci.py + noci2.py) plus a latent timectrl.py binning hazard — docs/develop/active/issues/KNOWN_BUGS.md

- 15:26 — plan-reviewer gated the a01_hiding_drivers nociception section: 2 Critical findings. (1) noci.py/noci2.py reconstruction off-by-one at episode boundary — the guard `src>=estart` feeds the spawn row's start injury into the reconstructed percept, but the env never writes reset injury into the buffer (core.py:115 first writes at step 1); ~5% of steps inflated up to +13.8 pts, and the all-zero 'felt 0' column of the dissociation table is an artifact (corrected cells read 0.5-9.9%). (2) Peri-event '+8pp at peak' rests on the mechanically depressed lag -1 baseline plus a collider-selecting isolated-event filter; the all-events curve shows only ~+1pp over its own pre-window baseline. The mechanical fact 'no damage while in bush' was independently VERIFIED in src+config (bush blocks_animals=true; ambush resources never respawn + disjoint reset placement; rocks overlap-only; 10.78M damage steps, 0 in bush). Corrected buffer-of-injury-levels mechanism confirmed in src. Owners: analysis author (fix guard, rerun, rewrite two subsections); bug-curator (record the off-by-one).

- 14:44 — plan-reviewer round-2 on the bush-dwell write-up (docs/experiments/active/trajectory_factors/a01_hiding_drivers.md): verdict SUPPORTED WITH CAVEATS, one must-fix — finding 4's 'randomised hunger does nothing immediately (18-20% flat)' is contradicted by the store (16.4% vs 21.4% in the first 10 steps; per-step gap grows to -8..-11pp by t=8-10). Correcting it strengthens the conclusion (hunger causally suppresses hiding). All other new numbers (false-alarm test, scent ladders, crux table, injury window) reproduced exactly.

- 14:29 — plan-reviewer gated the bush-dwell GLM interim verdict (tmp/20260825_bushdwell_glm_interim.md): C1-C3 supported with caveats (C2 rabbit-smell effect reproduced, contrast z=13.8), but C4's temporal-confound interpretation is refuted by its own finished time-controlled cross-tab (hunger gradient persists within every step bin), and the '+40pp rabbit-near' figure is a conditional coefficient from an endogenous multivariate model — the univariate association is negative. Findings returned inline to parent.


---

*See `docs/diary/README.md` for what each section is for.*
