# Diary — 2026-06-02

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| _(no sessions yet today)_ | | | | | |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 06:17 — experiment-analyzer: matched-aggression control eval analyzed (final 10M ckpt, 200-seed deterministic, Arm A distributional vs Arm B matched-aggression predator). Verdict: NO pre-contact predator-vs-rabbit discrimination — the earlier +2.5-cell pre-contact gap was 2 measurement confounds (2-rabbits-vs-1-predator pooling + slower-approaching distributional predator). With both removed (Arm B, 1-vs-1) pre-contact gap collapses to -0.07 to -0.24 (slightly negative); agent dies in 91% of episodes under matched chase. Discrimination is entirely post-contact pain-driven; no bug. Results + final synthesis written to [[sameprop_chasing_rabbit]] (docs/experiments/active/hypervigilance/sameprop_chasing_rabbit.md).

- 05:57 — experiment-designer: authored matched-aggression chasing-rabbit EVAL config [[06-matchedAggression_chasingRabbit_eval]] (configs/experiment/hypervigilance/06-matchedAggression_chasingRabbit_eval.yaml) — predator's 5 chase params fixed equal to the rabbits to remove the approach-speed confound; validated obs-parity (dim 27, breakdown byte-identical to 04) and 3-animal chase-param equality (predator==rabbits), damage asymmetry preserved. Eval not run. Pending env-config-auditor pre-flight.

- 01:10 — code-reviewer: obs-pipeline class-leak audit for chasing-rabbit experiments — verdict: predator/rabbit discrimination fully explained by 2 contact-only channels (visual ch 5-vs-7 + extero-nociception is_damaging mask); no hidden bug; eval-path parity PASS; noise modality sync PASS. visual_sensor_range=0 makes the class signal overlap-only, so discrimination is learned generalization not a distance label. Report: [chasingRabbit_obs_classLeak_audit](docs/reviews/chasingRabbit_obs_classLeak_audit.md)


---

*See `docs/diary/README.md` for what each section is for.*
