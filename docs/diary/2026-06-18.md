# Diary — 2026-06-18

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 00:12 | (open) | 96e71c7b-dc03-44c9-a98c-1c2acc86e0d9 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 15:25 | 96e71c7b/developer | implemented | explicit visual_properties in live v3.0 configs (byte-parity preserved) | [default](../../configs/environment/default.yaml) |
| 01:17 | 96e71c7b/developer | implemented | configurable per-entity visual properties (v3.0, static, default-8, byte-parity gate green) | [CONFIGURABLE_VISUAL_PROPERTIES_PLAN](../develop/active/sensors/CONFIGURABLE_VISUAL_PROPERTIES_PLAN.md) |
| 00:12 | 96e71c7b/developer | implemented | configurable initial-state randomization ranges (start_nutrition_low/high, start_injury_low/high) | [CONFIGURABLE_INITIAL_STATE_RANGES](../develop/active/refactors/CONFIGURABLE_INITIAL_STATE_RANGES.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `96e71c7b` — Config-system overhaul + both v3.0 refactors landed (init ranges + visual properties)

**Full session UUID** (for `claude --resume`): `96e71c7b-dc03-44c9-a98c-1c2acc86e0d9`

**What this session did** (in plain words):

Large config-system arc on v3.0, all verified green:
- Modernized `default.yaml` (unified `entities:` + `behavior_measures:`), then made it the canonical BASE other configs override via an opt-in `extends: environment/default` deep-merge chokepoint; moved `configs/experiment/` → `configs/environment/experiment/archive/` (91 configs, history preserved) + swept ~159 refs.
- Compacted the 200-line `eval_seeds` literal to a `{rng, sort}` generator spec (byte-identical), and authored a clean 5-level `basic/` curriculum as the first real `extends:` consumer.
- Landed BOTH paused refactors: **configurable initial-state ranges** (start the agent genuinely hungry/injured via opt-in `body.start_*_low/high`, conditional-mandatory) and **configurable per-entity visual properties** (visual sensor now reads config appearance vectors like olfaction; size configurable, default 8; class→channel map kept as hidden default generator; NEU/RCK label bug fixed).
- Each refactor went plan → developer → code-reviewer + env-config-auditor; both reviews APPROVE/PASS.

**Headline finding**:

default.yaml is now the canonical override-base; experiment configs moved under environment/ and archived; and both v3.0 refactors (full-range initial-state randomization + config-driven visual properties) shipped with byte-parity preserved — full env suite 157 passed / 0 failed.

**What's next**:

1. (Optional) experiment-designer: rewrite live experiment configs as sparse `extends:` files (mechanism proven, not yet widely used).
2. (Optional) Author opt-in configs that USE the new features: randomized hungry/injured starts; custom visual_properties / non-8 vector size.
3. (Optional) Address cosmetic nits: state.py field ordering; olfactory debug-overlay label list; render asset-resolver keyed on visual_properties (deferred out of v1).

**Detailed sources**:

- Plans: [`CONFIG_LAYERING_AND_EXPERIMENT_REORG`](../develop/active/refactors/CONFIG_LAYERING_AND_EXPERIMENT_REORG.md), [`CONFIGURABLE_INITIAL_STATE_RANGES`](../develop/active/refactors/CONFIGURABLE_INITIAL_STATE_RANGES.md), [`CONFIGURABLE_VISUAL_PROPERTIES_PLAN`](../develop/active/sensors/CONFIGURABLE_VISUAL_PROPERTIES_PLAN.md)
- Curriculum: [`basic_curriculum`](../experiments/active/basic_curriculum/basic_curriculum.md)
- Reviews: docs/reviews/config_extends_layering*, config_visual_properties_postimpl
- Commits: `e233359`,`c13a3ac`,`b509bb4`,`c066d2b`,`442abe4`,`078904b`,`ddff125`

## Notes
- 15:36 — research-postdoc wrote the symposium synthesis adjudicating the four-neuromodulator env-suitability question (NA suitable-as-is, ACh needs-env-work, DA suitable-with-config, 5-HT suitable-with-analysis-only) — [postdoc_synthesis](docs/project/symposium/20260618_env_suitability_neuromodulation/postdoc_synthesis.md)

- 01:32 — config-audit: configurable visual properties post-impl (ddff125) — PASS. All 6 checks clear. 15 parity+properties tests green. Safe to launch. See [config_visual_properties_postimpl](docs/reviews/config_visual_properties_postimpl.md)


---

*See `docs/diary/README.md` for what each section is for.*
