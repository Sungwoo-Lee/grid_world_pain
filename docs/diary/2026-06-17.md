# Diary — 2026-06-17

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 02:13 | (open) | 96e71c7b-dc03-44c9-a98c-1c2acc86e0d9 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 22:39 | 96e71c7b/developer | implemented | eval_seeds compact generator spec: replace 200-line literal list with {rng: 42, sort: true} in default.yaml | [default](../../configs/environment/default.yaml) |
| 02:13 | 96e71c7b/developer | implemented | CONFIG_LAYERING_AND_EXPERIMENT_REORG: extends: chokepoint + experiment config archive (91 configs moved, 19 fixtures renamed, all tests green) | [CONFIG_LAYERING_AND_EXPERIMENT_REORG](../develop/active/refactors/CONFIG_LAYERING_AND_EXPERIMENT_REORG.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `96e71c7b` — Config-system restructure: default.yaml as canonical base + experiment reorg

**Full session UUID** (for `claude --resume`): `96e71c7b-dc03-44c9-a98c-1c2acc86e0d9`

**What this session did** (in plain words):

- Modernized `configs/environment/default.yaml` to the unified `entities:` schema and added the `behavior_measures:` block; proved byte-identical environment generation against a pre-edit trajectory and the committed parity fixture.
- Made `default.yaml` the canonical BASE other configs override, via an opt-in top-level `extends: environment/default` key resolved at a single deep-merge chokepoint (`load_env_config` in `config_loader.py`). Configs without `extends:` load standalone exactly as before — this opt-in is what stops the base's content leaking into configs that omitted a block.
- Moved `configs/experiment/` to `configs/environment/experiment/archive/` (91 configs `git mv`'d, history preserved, byte-unchanged) and swept ~159 path references across code/scripts/tests.
- Flow: senior-developer authored the checkpointed plan, developer implemented, code-reviewer + env-config-auditor verified.

**Headline finding**:

default.yaml is now the canonical base experiment configs override via an opt-in extends: key; the experiment folder moved under environment/ with all 91 originals archived — full env suite 136 passed / 0 failed, parity green.

**What's next**:

1. (Optional) Have experiment-designer rewrite active experiment configs as sparse `extends: environment/default` files — the mechanism is proven but no config uses it yet.
2. Resume the two paused config refactors: configurable initial-state ranges and configurable visual properties (both authored, not started).
3. (Optional) Address code-reviewer nits: type-guard `extends:` values; raise (not warn) on a missing top-level config.

**Detailed sources**:

- Plan + Implementation Report: [`CONFIG_LAYERING_AND_EXPERIMENT_REORG`](../develop/active/refactors/CONFIG_LAYERING_AND_EXPERIMENT_REORG.md)
- Reviews: [`config_extends_layering`](../reviews/config_extends_layering.md), [`config_extends_layering_reorg_postimpl`](../reviews/config_extends_layering_reorg_postimpl.md)
- Commits: `e233359` (default.yaml modernize), `c13a3ac` (extends chokepoint + reorg), `01e54aa` (agent-profile paths), `4997ece`

## Notes

_(free-form bullets; no script involvement)_

---

*See `docs/diary/README.md` for what each section is for.*
