# Diary — 2026-06-23

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 16:25 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |
| 00:34 | (open) | 3c11010b-dbfb-4d17-af58-5b86e54d6815 | (auto — session-start was not called) | Auto-created on first event. |  |
| 00:27 | (open) | 428cfe9b-4b3e-4d99-8065-98b46ee41016 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 16:25 | 4efbe660 | insight | Nutrition sweep: hg10's eat-failure is driven by initial nutrition (runway) + a wander-away-from-adjacent-food policy, NOT animal avoidance — starves even with no animal at low nutrition; confounds the earlier hypervigilance read | [20260623_1623_nutrition_runway_confounds_hypervig_read](../memory/memories/hypervigilance/20260623_1623_nutrition_runway_confounds_hypervig_read.md) |
| 16:18 | 428cfe9b | insight | rPPO is recompile-IMMUNE to per-episode count-variance (resets all envs at fixed shape + jnp.where; K-activation traced + count_high-static masks; empirically 1 compile / 0 recompiles over 800 steps) — opposite of the Dreamer done-count-sized reset storm | [20260623_1616_rppo_reset_recompile_immune](../memory/memories/env_entities/20260623_1616_rppo_reset_recompile_immune.md) |
| 01:46 | 428cfe9b | insight | A contended-GPU proxy benchmark hid a real -13% step-SPS regression (driven by +2 animal hot-path slots) that a clean idle-node measure revealed; reset +28% negligible; rabbit trimmed 3->2 to recover ~half | [20260623_0145_proxy_benchmark_contention_misestimate](../memory/memories/env_entities/20260623_0145_proxy_benchmark_contention_misestimate.md) |
| 01:46 | 428cfe9b | insight | Blocker: inactive resource slots revived on step 1 (update_resources overloaded res_active=False as 'eaten, regrow'); a per-episode mask must cover the respawn path; fix = immutable res_allocated mask; both reviewers caught it | [20260623_0144_inactive_resource_slots_revive_respawn](../memory/memories/env_entities/20260623_0144_inactive_resource_slots_revive_respawn.md) |
| 01:46 | 428cfe9b | insight | Per-episode entity-count variance: JAX static shapes -> allocate count_high + per-episode activate K~U[low,high] via res_active/animal_active/obs_active masks; whole-grid default spawn; scalar count backward-compat; two-commit parity discipline | [20260623_0143_per_episode_count_variance_masking](../memory/memories/env_entities/20260623_0143_per_episode_count_variance_masking.md) |
| 00:34 | 3c11010b/developer | implemented | revert recompile-storm fix in dreamer_srl_main.py (GPU compile regression) | [recompile_storm_fix](../develop/active/dreamer_srl_v2/recompile_storm_fix.md) |
| 00:27 | 428cfe9b/developer | implemented | fix resource-revival blocker in PER_EPISODE_ENV_VARIANCE: res_allocated gate in update_resources + rabbit count_high 3→2 trim + 2 default fixture regen | [PER_EPISODE_ENV_VARIANCE](../develop/active/refactors/PER_EPISODE_ENV_VARIANCE.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `428cfe9b` — Olfactory-ambiguity training program + per-episode env-variance feature

**Full session UUID** (for `claude --resume`): `428cfe9b-4b3e-4d99-8065-98b46ee41016`

**What this session did** (in plain words):

- Designed + launched the **olfactory-ambiguity** discrimination sweep (10 runs, smell separation s x per-episode noise sigma) and analyzed it: discrimination is weak (never reaches the 0.5-cell bar), agent is reactive tank-and-hide, hunger-gating refuted but "lethality masks gating".
- Launched two follow-ups: a **hypervigilance scarce-vs-abundant** pair (both done) and a **linear-olfactory-decay** re-run of the sweep (decay_power 2.0->1.0; 10 runs, training).
- Renamed `hunger_gated/` -> `olfactory_ambiguity/` (+ lindecay sibling) and added per-folder README sweep tables.
- Shipped the **per-episode environment-variance** feature: randomized entity counts (count_low/high via static-max allocation + activation masks) + whole-grid default spawn. Fixed a resource-revival blocker (both reviewers caught it) and benchmarked the speed cost on a clean idle node.

**Headline finding**:

Per-episode env-variance shipped (counts as ranges + whole-grid spawn, ~7% step-SPS cost after trimming rabbit); olfactory-ambiguity sweeps show predator-vs-rabbit discrimination stays weak and lethality masks the hunger-gate.

**What's next**:

1. When the linear-decay sweep finishes (~06-23), analyze it paired vs the decay-2.0 surface — does a longer-range smell cue produce pre-emptive avoidance?
2. Analyze the hypervigilance scarce-vs-abundant pair (both trained to 10M).
3. Future training inherits the new variance-rich default; optionally re-benchmark the trimmed config's step-SPS on a clean node.

**Detailed sources**:

- Insights: `env_entities/` (3, env-variance), `hypervigilance/` (4, olfactory-ambiguity) captured this session
- Plans/designs: [`PER_EPISODE_ENV_VARIANCE`](../develop/active/refactors/PER_EPISODE_ENV_VARIANCE.md), [`20260619_hunger_gated_step1_discrimination_onset`](../experiments/active/hypervigilance/20260619_hunger_gated_step1_discrimination_onset.md)
- Commits: `90d687f`, `7fa6183`, `61e0c73` (env-variance); `cf8d23b` (rename), `b15b62a` (READMEs)

## Notes
- 16:18 — **Cross-session heads-up for the Dreamer / recompile-storm work:** the new per-episode env count-variance (configs/environment/default.yaml count_low/high + activation masks) is recompile-SAFE under rPPO — recurrent_ppo resets ALL envs every step at fixed (num_envs) shape then jnp.where(done,...), and K-activation is a traced scalar with count_high-static masks (empirically 1 compile / 0 recompiles over 800 steps spanning many resets). This is the OPPOSITE of the Dreamer done-count-sized reset storm ([[20260622_1746_dreamer_srl_recompile_storm_done_count]]). If Dreamer adopts the variance feature, keep the reset-all-at-fixed-shape-then-where pattern. Details: [20260623_1616_rppo_reset_recompile_immune](../memory/memories/env_entities/20260623_1616_rppo_reset_recompile_immune.md)


---

*See `docs/diary/README.md` for what each section is for.*
