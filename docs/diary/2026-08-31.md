# Diary — 2026-08-31

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 19:49 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Sensor ladder rebuilt on 1M episodes/arm; one finding retracted

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- Tripled the evaluation population from 300,000 to 1,000,000 episodes per arm, using lab nodes 101/103/104/105 alongside the local 4090s. Finished 17:25 against a 19:14 single-node estimate. Final: 14M episodes, 3.32 billion step rows, verified contiguous and identical across all fourteen arms.
- Rebuilt all three derived products, all 15 figures, all 10 tables and the published page, then compared 20 headline quantities against the 300k values.
- Sixteen moved by less than their rounding. The main result — four independent measures separating nine arms from five with no overlap — holds with margins essentially unchanged.
- One finding was retracted. The hypervigilance claim held in 14/14 arms at 300k and in only 12/14 at 1M, and the clean nine-vs-five split on that measure did not survive; both documents now carry an explicit correction and the finding is re-badged as the weakest on the page.
- Built a render-based artifact format gate (headless Chrome, `scripts/claude/check_artifact_layout.py`), a defect register with 15 entries, and an `artifact-format-reviewer` agent that runs before every publish. It immediately caught a mislabelled headline figure that two static reviews had missed.

**Headline finding**:

Tripling the evaluation sample retracted one finding (hypervigilance: 14/14 arms became 12/14, and the group split behind it was an artefact of the smaller sample) while leaving the main nine-vs-five discrimination result untouched.

**What's next**:

1. Replicate the ladder at 2-3 seeds. All fourteen arms share seed 42, so no effect size here is separable from training-run variance — this is now the only thing standing between the study and confirmatory status.
2. Address the two open items from the analysis review: the predator-odour control is contaminated by the intensity/discriminability anticorrelation, and identity is confounded with input capacity in Q1/Q2.
3. Use the artifact-format-reviewer gate on every future artifact; it is registered in CLAUDE.md.

**Detailed sources**:

- Report: [`sensor_ladder`](../experiments/active/sensor_ladder/sensor_ladder.md) — 15 figures, 10 generated tables
- Format register: [`artifact_format_bugs`](../develop/active/meta/artifact_format_bugs.md) — 15 defect classes, with the render-based protocol
- Analysis review: [`plan_sensor_ladder`](../reviews/plan_sensor_ladder.md)
- Commits: `daf30a46` (1M rebuild + retraction), `13ce99f6` (format gate), `da44bdce` (layout fixes)

## Notes
- 16:10 — bug-curator: recorded the repo-root `save_snapshot.py` bug (hand-built modulation config + missing encoding config — raises on every config today) as OPEN/Low in [Known Bugs](docs/develop/active/issues/KNOWN_BUGS.md); cross-linked the sibling fixed row (Finding A, `2ad9104`). Commit `55bcce54`.

- 15:34 — plan-reviewer gated MODULATION_SITE_REFACTOR — SOUND WITH CONCERNS, 1 Critical: the plan's PAPL-precondition claim (reward not a function of injury) is false — verified in src/environment/core.py, the reward is the drive reduction over (satiation, injury); rewrite Analysis §F bullet 3 + Risks item 3 before approval. Also: archived-run eval tools load the run's saved config.yaml, so the temp_clip hard error blocks re-analysis post-migration — needs a decided policy. Review: docs/reviews/plan_modulation_site_refactor.md


---

*See `docs/diary/README.md` for what each section is for.*
