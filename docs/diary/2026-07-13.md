# Diary — 2026-07-13

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 17:56 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Regenerated 32 dwell curves + corrected eval-sweep diagnosis

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

Root-caused the eval-sweep 'NAS jam' as a mis-diagnosis: cost is dominated by multithreaded XLA compile (~13s/proc, threadpools sized to full core count), so a few procs oversubscribe CPU (16->load 100 on 20 cores); the load-137 freeze was that oversubscription x stacked un-killable workers. Fix (measured 5x, load 100->20): persistent XLA compile cache + 1-thread cap per proc + NPAR~=cores. Corrected LAB_NODE_GPU_SPEC.md (8de24a0). Regenerated 32 dwell curves: 28 units fanned across nodes 101-105 (one thread-capped worker each, LPT-balanced ~3.7k evals/node, ~1h15m) + a pred_inj00 incremental refresh. Final verified: 32/32 CSVs + 32/32 PNGs, all non-empty.

**Headline finding**:

Eval sweep is CPU-bound (multithreaded XLA compile), NOT NAS-bound as previously claimed; fixed the diagnosis and regenerated all 32 dwell-history curves 5x faster.

**What's next**:

None required; results/eval/avoidance/b0{2..5}_128env_dwell_history/ hold the 8-condition dwell curves for analysis.

**Detailed sources**:

docs/environment/LAB_NODE_GPU_SPEC.md,results/eval/avoidance/b05_128env_dwell_history/

## Notes
- 18:26 — Added 5 rPPO size-variant configs (XS/S/M/L/XL) under configs/models/recurrent_ppo/, mirroring DreamerV3 XS→XL presets by hidden_size + encoder default_mlp; standalone copies since agent configs don't support extends. Commit e1fd94f.


---

*See `docs/diary/README.md` for what each section is for.*
