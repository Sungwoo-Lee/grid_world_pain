# Diary — 2026-05-29

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 18:28 | (open) | ba4a22ee-e03f-4d94-a463-bb7d83fd8642 | (auto — session-start was not called) | Auto-created on first event. |  |
| 02:09 | (open) | 7962c4de-7ac9-4c9a-9958-c22a36fd45c7 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 18:29 | 7962c4de | insight | 4 dreamer-srl cells crashed at ep 10000 with AttributeError on EnvState.animal_pos. Root cause: src/utils/eval_recording.py is lazy-imported AFTER a parallel session pulled the CP6 EnvState unification mid-training — interpreter had OLD class cached, lazy import loaded NEW file from disk. | [20260529_1826_lazy_import_schema_drift_first_call_crash](../memory/memories/dreamer_diagnosis/20260529_1826_lazy_import_schema_drift_first_call_crash.md) |
| 18:29 | 7962c4de | insight | log_interval should be anchored to ~140 WandB rows / 24h session, not env-steps-per-row. Dreamer-srl reaches 250-500x fewer episodes than rPPO in the same wall-clock; XS/num_envs=16 cadence is log_interval=2000 (was 50000 — gave 4-6 rows/session, useless). | [20260529_1825_log_interval_anchored_rows_per_session](../memory/memories/cluster_ops/20260529_1825_log_interval_anchored_rows_per_session.md) |
| 18:29 | 7962c4de | insight | Lifted rPPO's spread pattern into dreamer_srl_main.py so wandb UI filter agent.algorithm = DreamerV3 works cleanly; added env_config.yaml + agent_config.yaml save to results/models/ for reproducibility parity with rPPO. | [20260529_1824_dreamer_srl_wandb_spread_and_config_save_parity](../memory/memories/cluster_ops/20260529_1824_dreamer_srl_wandb_spread_and_config_save_parity.md) |
| 18:28 | ba4a22ee | insight | v2.0 env merges predator + neutral animals into one unified entity class with a static class tag (sensor channels + metric fan-out only) and adds per-episode uniform distributional sampling on 5 core behavioural fields. Shipped via CP1-CP6 atomic refactor across 86 configs; PRNG byte-parity vs v1.4 preserved by a per-subset call pattern. | [20260529_1823_unified_animal_entity_v2_0_arch](../memory/memories/env_entities/20260529_1823_unified_animal_entity_v2_0_arch.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| 02:09 |  | 7962c4de/training-runner | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s45_n113_gpu1_log2k | 113:1 | running | 4 | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s45_n113_gpu1_log2k | — | [NMN_CONTINUAL_DOUBLE_RETURN_PROBE](../experiments/active/hypervigilance/NMN_CONTINUAL_DOUBLE_RETURN_PROBE.md) |
| 02:09 |  | 7962c4de/training-runner | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s44_n113_gpu0_log2k | 113:0 | running | 3 | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s44_n113_gpu0_log2k | — | [NMN_CONTINUAL_DOUBLE_RETURN_PROBE](../experiments/active/hypervigilance/NMN_CONTINUAL_DOUBLE_RETURN_PROBE.md) |
| 02:09 |  | 7962c4de/training-runner | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s43_n114_gpu1_log2k | 114:1 | running | 2 | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s43_n114_gpu1_log2k | — | [NMN_CONTINUAL_DOUBLE_RETURN_PROBE](../experiments/active/hypervigilance/NMN_CONTINUAL_DOUBLE_RETURN_PROBE.md) |
| 02:09 |  | 7962c4de/training-runner | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s42_n114_gpu0_log2k | 114:0 | running | 1 | dreamer_srl_v2_postfix_XS_envs_16_ep10M_buf256k_s42_n114_gpu0_log2k | — | [NMN_CONTINUAL_DOUBLE_RETURN_PROBE](../experiments/active/hypervigilance/NMN_CONTINUAL_DOUBLE_RETURN_PROBE.md) |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `ba4a22ee-e03f-4d94-a463-bb7d83fd8642` — v2.0 env_entities ship + R3 sameProp-predator-distributional launch + 2 mid-training previews

**Full session UUID** (for `claude --resume`): `ba4a22ee-e03f-4d94-a463-bb7d83fd8642`

**What this session did** (in plain words):

- **Closed the v2.0 env_entities refactor end-to-end.** Across 2026-05-28 (CP1-CP6) the predator-vs-neutral two-class environment was unified into a single animal-entity class whose `class` field is a static tag used only for sensor channels and metric fan-out, with all behaviour and damage now programmable per entity. The YAML schema additionally accepts `[low, high]` ranges on five core behavioural fields (`detection_range`, `max_stamina`, `stamina_recovery_rate`, `hunt_stamina_threshold`, `lose_interest_multiplier`) sampled uniformly per-episode, per-entity. 86 configs migrated, 228 tests pass, PRNG byte-parity vs v1.4 preserved by a per-subset call pattern. Plan revised through v0.1 -> v0.4 with two code-reviewer rejects (B1 masked-combine, then N1-N3 jax_reset PRNG threading) before sign-off.
- **Documented the bg-session Edit/Write isolation guard and its 4-tier workaround hierarchy.** The user surfaced the question after seeing me launch R3 training despite the guard. Created an in-repo meta doc (`docs/develop/active/meta/background_session_isolation.md`), an auto-memory feedback file (`~/.claude/.../memory/feedback_background_session_edit_isolation.md`), and a memory insight (`subagent_engineering/20260528_1647_bg_isolation_subagent_bypass`) extending the prior 2026-05-19 finding with the sub-agent-bypass discovery from CP1-CP6.
- **Designed and launched the R3 sameProp-predator-distributional experiment.** Pre-registered question: if the predator's behavioural parameters are no longer static, does the agent generalise its avoidance behaviour to neutral animals? Locked headline metric = reduced class gap on M2 (in-cover rate at threat-onset). Baseline re-uses R2.6 Cell C; predator-only distributional on the 5 fields above (rabbit kept fixed); matched smells. Launched on n113:0 (seed 46, WandB tag `hypervigilance-round3-distributional-seed46`, run ID `0ikqqpvc`) on 2026-05-28 16:18; training is currently at ep 3.9M / 10M (39%) and projected to close ~2026-05-31.
- **Ran two mid-training previews of R3 via experiment-analyzer.** First preview at ep 1.81M (18%): Delta_M2_class = +35.1 pp -- H_zero(class-pinned) band, R2.6 baseline +37 pp. Second preview at ep 3.90M (39%): Delta_M2_class = +31.6 pp, 1.6 pp above the H_zero refutation floor. Rabbit-side M2 caught up faster than predator-side (+5.2 pp vs +1.8 pp); M5_predator strategy-reshape half-relaxed (0.560 -> 0.660 toward baseline 0.769). Linear extrapolation projects closing Delta_M2 ~+21 pp -- borderline band, which would trigger pre-registered seed-47 escalation.
- **Captured one memory insight (env_entities/unified_animal_entity_v2_0_arch).** Documents the architecture lock + 5 distributional fields + per-subset PRNG byte-parity technique + 86-config migration as the canonical v2.0 env shape. New folder `env_entities` opened (definition: "Env entity architecture decisions").

**Headline finding**:

v2.0 env_entities refactor shipped end-to-end (CP1-CP6); R3 sameProp-predator-distributional launched (39% trained); Delta_M2_class trajectory +35.1 -> +31.6 pp narrowing toward borderline-band closing projection (~+21 pp)

**What's next**:

1. **Closing R3 analysis** at end of training (~2026-05-31). Pre-registered: H_one confirmation if Delta_M2_class < +20 pp AND (M2_neutral >= 0.65 OR M5_neutral < 1.05). Borderline if +20 <= Delta_M2 < +30 -- triggers seed-47 escalation.
2. **Pre-position seed-47 escalation config + design doc skeleton** while training runs, so the closing analyst can launch immediately if the verdict lands borderline.
3. **Wire `Episode/sampled_*_<tag>` WandB logging into `train.py`** (CP5 follow-up; `src/behavior/accumulators.py` already builds the dict via `build_episode_log_dict` + `sampled_wandb_keys`).
4. **Investigate M5 per-tag rabbit fan-out drift** (rabbit_TL=1.099 vs rabbit_BR=1.358, gap 0.259) at closing -- could be a code-side artefact of the unified-entity refactor or a sample-noise artefact.
5. **Low-priority backlog**: D-CP6-1 legacy info-dict alias removal; `perceptual_noise.enabled` mandatory promotion (after `configs/models/` sweep settles); R3 1000-step PPO speed validation (the R3 launch itself serves this).

**Detailed sources**:

- Plan: [`UNIFIED_ANIMAL_ENTITY_AND_PER_EPISODE_SAMPLING`](../develop/active/env_entities/UNIFIED_ANIMAL_ENTITY_AND_PER_EPISODE_SAMPLING.md)
- R3 design + bands + previews: [`sameprop_predator_distributional`](../experiments/active/hypervigilance/sameprop_predator_distributional.md) (sections 9-15)
- bg-isolation meta doc: [`background_session_isolation`](../develop/active/meta/background_session_isolation.md)
- Memory: [[20260529_1823_unified_animal_entity_v2_0_arch]], [[20260528_1647_bg_isolation_subagent_bypass]]
- WandB: hypervigilance-round3-distributional-seed46 (run 0ikqqpvc) -- https://wandb.ai/sungwoolee/grid_world_pain/runs/0ikqqpvc
- Commits: CP1 `4ef0b6b`, CP3 `a85a951`, CP4 `6685d0e`, CP5 `b783bca`, CP6 `3653247`; plan errata `cddf6f2`; R3 config land `e3e6789`; bg-isolation meta `cc4eb96`; bg-isolation memory `2076ba7`; R3 first preview `d706948`, second preview `22940b9`

---

### Session `7962c4de` — dreamer-srl post-fix relaunch + WandB / config-save parity + log_interval cadence rule

**Full session UUID** (for `claude --resume`): `7962c4de-7ac9-4c9a-9958-c22a36fd45c7`

**What this session did** (in plain words):

Diagnosed the cascade of failures across 4 dreamer-srl cells launched 2026-05-28 03:30 KST: all 4 crashed at episode 10000 with AttributeError on EnvState.animal_pos, root-caused to a lazy import in src/algorithms/dreamer_srl/eval.py:68 racing a parallel session's CP6 EnvState unification (predator_pos / neutral_pos → unified animal_pos) — the interpreter had cached the OLD class but the lazy import loaded the NEW eval_recording.py from disk at first checkpoint. Re-launched 4 fresh cells (w9as1qe3 s42 n114:0, x0ahp61j s43 n114:1, jx3obafg s44 n113:0, h7aa37na s45 n113:1) on fully-unified v2.0 with two new fixes baked in: (1) log_interval lowered 50000→2000 in the YAML — anchored to ~140 WandB rows / 24h session instead of env-steps-per-row, after observing the prior 50k cells gave only 4-6 rows / 22k-episode session because Dreamer-srl reaches 250-500x fewer episodes than rPPO in the same wall-clock; (2) WandB config now uses rPPO's **config.to_dict() spread pattern so 'agent.algorithm = DreamerV3' filters cleanly in the UI (pre-fix the entire agent YAML was wrapped under wandb.config['agent'], producing doubly-nested agent.agent.algorithm that the filter couldn't reach), and env_config.yaml + agent_config.yaml are now saved to <results_dir>/models/ for reproducibility parity with rPPO. Smoke-validated via 200-env-step run mo1pkvm5 — wandb.config['agent'] == {'algorithm': 'DreamerV3'}, both YAMLs landed well-formed.

**Headline finding**:

4 cells relaunched 02:09 KST on fully-unified v2.0 with WandB spread-pattern + log_interval=2000 + dual config-save baked in. Smoke confirmed clean wandb.config.agent. Crash-cause was lazy-import schema drift, not a real code bug.

**What's next**:

Watch the 4 cells through first checkpoint (ep 10000) to confirm no AttributeError recurrence (~7h post-launch, so ~09:00 KST today). At ~12h verify ~70-100 WandB rows landed to validate the log_interval=2000 rule of thumb. User may want to terminate the 4 in-flight cells and relaunch on the post-fix code so they ALSO get clean WandB config + saved YAMLs — open decision.

**Detailed sources**:

docs/develop/active/dreamer_srl_v2/wandb_filter_and_config_save_parity.md | configs/models/dreamer_srl/01_food_only_buf256k.yaml:130-145 | src/algorithms/dreamer_srl/dreamer_srl_main.py:386-419 | src/utils/eval_recording.py:30-39 | docs/memory/memories/cluster_ops/20260529_1824_dreamer_srl_wandb_spread_and_config_save_parity.md | docs/memory/memories/cluster_ops/20260529_1825_log_interval_anchored_rows_per_session.md | docs/memory/memories/dreamer_diagnosis/20260529_1826_lazy_import_schema_drift_first_call_crash.md | commits 0de7fe4 (log_interval) + 2356c33 (WandB spread + config save) | smoke run mo1pkvm5

## Notes
- 21:03 — experiment-designer: authored R4 'chasing rabbit' rPPO experiment — [04-sameProp_R4_chasingRabbit.yaml](configs/experiment/hypervigilance/04-sameProp_R4_chasingRabbit.yaml) (rabbits become behaviour:hunt, harmless neutral class, full-grid patrol so they chase across the map; predator == R3) + design doc [sameprop_chasing_rabbit](docs/experiments/active/hypervigilance/sameprop_chasing_rabbit.md). Loader validation passed. Not launched; env-config-auditor pre-flight pending.

- 16:13 — experiment-analyzer (ba4a22ee): R3 sameprop-predator-distributional SECOND MID-TRAINING preview at ep 3.90M / 10M (39%). Training still running on n113:0 (PID 655905, not interrupted). Headline trajectory vs first preview (§§9-11 at 18%): Δ_M2_class +35.1 → +31.6 pp (gap narrowing; still H₀-band but thin margin, 1.6 pp above floor). Rabbit-side M2 caught up: 34.3% → 39.5% (+5.2 pp) while predator-side moved 69.3% → 71.1% (+1.8 pp). M5_predator relaxed from 0.560 → 0.660 — strategy-reshape pattern faded by half toward baseline 0.769. Survival converged to baseline within noise (411.9 ± 121.9 vs 414.7 ± 118.3). Closing projection now spans H₀-thin / borderline / H₁-edge. Doc: docs/experiments/active/hypervigilance/sameprop_predator_distributional.md §§12-15 — commit 22940b9.

- 02:33 — experiment-analyzer (ba4a22ee): R3 sameprop-predator-distributional MID-TRAINING preview at iter 36,144 / 10M (ep 1.81M, 18%). Training still running on n113:0 (PID 655905, not interrupted). Headline: Δ_M2_class = +35.1 pp (R2.6 baseline +37 pp), in H₀(class-pinned) band at this waypoint. M2_pred=69.3%, M2_rab=34.3%, M5_pred=0.560 (more suppressed than R2.6's 0.769). Per-episode predator threat-fraction std 0.219 vs R2.6 0.148 — proxy confirms per-episode sampling fires. Closing verdict deferred. Doc: docs/experiments/active/hypervigilance/sameprop_predator_distributional.md §§9-11 — commit d706948.


---

*See `docs/diary/README.md` for what each section is for.*
