# Diary — 2026-07-26

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 04:20 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 04:20 | 4efbe660 | insight | Sub-agent backgrounding a driver then returning: orphaned driver + idle-ping storm; use parent-side monitor + SendMessage | [20260726_0420_subagent_bg_job_orphan_idle_ping](../memory/memories/subagent_engineering/20260726_0420_subagent_bg_job_orphan_idle_ping.md) |
| 04:20 | 4efbe660 | insight | experiment-eval opt-in moved to config-layer --eval-config selector + self-documenting defaults | [20260726_0419_experiment_eval_config_layer_selector](../memory/memories/config_system/20260726_0419_experiment_eval_config_layer_selector.md) |
| 04:20 | 4efbe660 | insight | Dreamer imagination dream-strip (visualize_dream.py): anchor t=0 one step before spatial contact; predator=nociceptive vs rabbit=harmless | [20260726_0418_dreamer_imagination_dream_strip_contact_anchor](../memory/memories/behavior_measures/20260726_0418_dreamer_imagination_dream_strip_contact_anchor.md) |
| 04:20 | 4efbe660 | insight | Dwell-sweep incremental re-run: stale done-markers false-complete + --max-checkpoints watermark poisoning; both silently skip work | [20260726_0417_dwell_sweep_rerun_silent_skip_traps](../memory/memories/behavior_measures/20260726_0417_dwell_sweep_rerun_silent_skip_traps.md) |
| 04:20 | 4efbe660 | insight | CONFIG_CRITICAL_SETTINGS.md: critical-settings registry + dated change log guardrail against silent config drift; wired into 5 agents + CLAUDE.md | [20260726_0416_critical_settings_registry_changelog_guardrail](../memory/memories/config_system/20260726_0416_critical_settings_registry_changelog_guardrail.md) |
| 04:20 | 4efbe660 | insight | sensory.decay_power silently drifted 1.0->2.0 in an unrelated 2026-02-27 commit; found via saved config, reverted to 1.0 | [20260726_0415_decay_power_silent_drift_reverted_1p0](../memory/memories/config_system/20260726_0415_decay_power_silent_drift_reverted_1p0.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — decay_power silent-drift caught + config-governance doc + results refresh + dream-viz

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- Shipped the eval-during-training config restructure (config-layer `--eval-config` selector + self-documenting defaults); relaunched node-110 cleanly on it.
- Updated behavior-metrics for all current training agents (rPPO GAE + NMN g32, Dreamer 16-vs-128-env) and rendered 144 Dreamer behavior videos.
- Found + fixed a dwell-sweep bug: stale `done_<node>` markers made re-runs false-complete in ~1s and stack workers; also hit & recovered a `--max-checkpoints` watermark-poisoning gotcha.
- **Caught that `sensory.decay_power` had silently drifted 1.0→2.0** in an unrelated 2026-02-27 refactor; reverted to 1.0.
- Created `docs/environment/CONFIG_CRITICAL_SETTINGS.md` (critical-settings registry + dated change log) to prevent silent config drift; wired as required reading into 5 config/training agents + CLAUDE.md; made it context-efficient.
- Generated Dreamer imagination dream-strip figures anchored one step before predator/rabbit contact.

**Headline finding**:

The olfactory distance-falloff exponent sensory.decay_power had silently drifted from the intended 1.0 to 2.0 inside an unrelated Feb-2026 'scale to 10x10' commit — reverted to 1.0 and added a registry + change-log guardrail so a silent flip can't recur.

**What's next**:

1. Decide whether to re-train the current 2.0-trained rPPO + Dreamer runs under the new 1.0 default (old models need `decay_power: 2.0` pinned to eval faithfully).
2. Optional: add a harness-level auto-memory rule to consult `CONFIG_CRITICAL_SETTINGS.md`.
3. Optional: fix the pre-existing broken link in `training-runner.md:20`; generate b03_128env dream figures / a predator-vs-rabbit contact sheet.

**Detailed sources**:

- Insights: [`decay_power_silent_drift`](../memory/memories/config_system/20260726_0415_decay_power_silent_drift_reverted_1p0.md), [`critical_settings_registry`](../memory/memories/config_system/20260726_0416_critical_settings_registry_changelog_guardrail.md), [`dwell_sweep_rerun_traps`](../memory/memories/behavior_measures/20260726_0417_dwell_sweep_rerun_silent_skip_traps.md), + 3 more this capture
- Doc: [`CONFIG_CRITICAL_SETTINGS`](../environment/CONFIG_CRITICAL_SETTINGS.md)
- Commits: `fff6055` (eval-config), `b3e20fb` (marker fix), `d6240f5` (doc+revert), `4f6202b` (compact), `69eb081` (agent wiring), `12cc70f` (memory)

## Notes
- 04:28 — Terminated all 4 Dreamer runs (they had decay_power 2.0, saved-config verified) and relaunched a 2x2 size comparison under the reverted canonical decay_power 1.0 (commit d6240f5, 2026-07-26 03:48 — all prior runs predated it). New grid on node 114, all seed 0, num_envs 128, replay_ratio 0.0625, --episodes 100000000, buffer cpu, ckpt+video every 1000 eps keep-all: 114:0 dsrl_b03_M_dp1 (qf721efb), 114:1 dsrl_b03_XS_dp1 (fwze090k), 114:2 dsrl_b04_M_dp1 (lco3j3w8), 114:3 dsrl_b04_XS_dp1 (g148xn28). decay_power 1.0 CONFIRMED in all four saved env_configs. Size choice: XS is sheeprl's smallest overlay — Dreamer XS 3.17M params vs Dreamer M 20.7M (6.5x) vs rPPO XS 1.28M. Exact rPPO parity is impossible since Dreamer carries a world model rPPO has no counterpart for (2.45M of XS's 3.17M); XS's actor+critic alone is 723K, BELOW rPPO XS's 1.28M total. XS and M differ ONLY in architecture — same replay_ratio/learning_starts/seq_len/batch/buffer.

- 00:51 — dsrl_b04_M_128env_n114 did NOT crash — it COMPLETED its budget: launched with --total-steps 50000000, it hit exactly 50M env-steps (390,625 iters x 128 envs), 387,412 episodes, 48.07h at 288.9 sps. WandB still showed 'crashed' only because its uploader thread had died ~18h earlier, so the finish signal never arrived. Root cause of the early stop: --total-steps silently switches Dreamer's dual-mode loop OUT of episode-based termination. Fixed both halves (commit 166f261): (1) configs/train/dreamer_srl.yaml now sets episodes: 100000000 matching rPPO, and the driver WARNs loudly when --total-steps disables episode mode; (2) implemented resume — new --load-checkpoint/--load-episode + restore_dreamer_training_state() mirroring rPPO's fatal-on-failure restore-to-target pattern. Handles two real gaps: the replay buffer is not checkpointed (gradients held off for max(learning_starts, seq_len+1) iters while the RESTORED policy refills it) and optimizer state was never saved (now saved; old checkpoints auto-detected and fall back to network-only restore). 7/7 checkpoint tests pass. Relaunched b04 from episode 380002 on node 114 GPU 1 as dsrl_b04_M_rr0p0625_128env_resume (WandB 2j2z44qy) with --episodes 100000000.


---

*See `docs/diary/README.md` for what each section is for.*
