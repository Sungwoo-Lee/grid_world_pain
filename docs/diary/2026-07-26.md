# Diary — 2026-07-26

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| _(no sessions yet today)_ | | | | | |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 00:51 — dsrl_b04_M_128env_n114 did NOT crash — it COMPLETED its budget: launched with --total-steps 50000000, it hit exactly 50M env-steps (390,625 iters x 128 envs), 387,412 episodes, 48.07h at 288.9 sps. WandB still showed 'crashed' only because its uploader thread had died ~18h earlier, so the finish signal never arrived. Root cause of the early stop: --total-steps silently switches Dreamer's dual-mode loop OUT of episode-based termination. Fixed both halves (commit 166f261): (1) configs/train/dreamer_srl.yaml now sets episodes: 100000000 matching rPPO, and the driver WARNs loudly when --total-steps disables episode mode; (2) implemented resume — new --load-checkpoint/--load-episode + restore_dreamer_training_state() mirroring rPPO's fatal-on-failure restore-to-target pattern. Handles two real gaps: the replay buffer is not checkpointed (gradients held off for max(learning_starts, seq_len+1) iters while the RESTORED policy refills it) and optimizer state was never saved (now saved; old checkpoints auto-detected and fall back to network-only restore). 7/7 checkpoint tests pass. Relaunched b04 from episode 380002 on node 114 GPU 1 as dsrl_b04_M_rr0p0625_128env_resume (WandB 2j2z44qy) with --episodes 100000000.


---

*See `docs/diary/README.md` for what each section is for.*
