# Diary — 2026-08-06

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 01:55 | (open) | 87d7d010-fa7d-466e-9113-1a7618f94d95 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `87d7d010` — GATE 2 PASSED — dreamer/train.py integration proven computationally identical

**Full session UUID** (for `claude --resume`): `87d7d010-fa7d-466e-9113-1a7618f94d95`

**What this session did** (in plain words):

- Finalized Gate 2 on both legs. Statistical leg: production-scale A/B on node 106 landed inside the measured noise-floor envelope on all six criteria; the -4.6 percent speed reading was traced to the GPU slot, not the code.
- Deterministic leg: after seeding the replay sampler on both trees (the audit's only must-fix) and pinning GPU determinism, a three-run set on node 114 (old+fix, its replicate, new+fix) came out BIT-IDENTICAL at full 20,000-episode scale — every per-episode record, 781,424 gradient steps, all 39 final-loss entries, and all 14 checkpoint modules including the PRNG key.
- Plan-reviewer audited the verdict against raw artifacts (comparator non-vacuity, artifact provenance, honest decision-rule reading): CONCLUSION SUPPORTED; one disclosure sentence being added.
- Side products now permanent: dreamer is same-seed reproducible (sampler fix lands with the integration), rPPO confirmed already reproducible, unknown-algorithm hang fixed, eval-video telemetry proven in production.
- Discovered the Gate-3 deferral condition already satisfied: the live 3-stage curriculum run ended 2026-08-03 (its diary row was stale).

**Headline finding**:

The integrated train.py path and the legacy dreamer entry point are the same computation: three 19-hour GPU runs (old, old-replicate, new) finished bit-identical in every logged number and every network weight.

**What's next**:

1. Gate 3 on user go: deprecation shim, launch-template flip, 19-config rename, merge of the integration branch + throwaway-tree cleanup.
2. bug-curator close-outs on landing: sampler-reproducibility row (fixed), unknown-algorithm hang (fixed), bench_sps rows.
3. Post-landing follow-ups: sampler-state-in-checkpoint refinement, eval_stats_num_envs dead key, remaining P2 backlog.

**Detailed sources**:

- Analysis: [gate2_ab_analysis](../experiments/active/dreamer_integration/gate2_ab_analysis.md)
- Plan: [DREAMER_SRL_TRAIN_PY_INTEGRATION_PLAN](../develop/active/dreamer/DREAMER_SRL_TRAIN_PY_INTEGRATION_PLAN.md)
- Commits: `cbe5b70`, `39c93ac`, `809c1a9` (+ fix-set commits on the two worktree branches)

## Notes

_(free-form bullets; no script involvement)_

---

*See `docs/diary/README.md` for what each section is for.*
