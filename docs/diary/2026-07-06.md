# Diary — 2026-07-06

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 03:33 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Ladder6 avoidance result-tables + videos (mid-training snapshot)

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

Archived all previous avoidance eval outputs into results/eval/avoidance/archive_pre_ladder6_20260706/, then ran the 12-condition x 30-episode avoidance probe on the current checkpoints of all 6 live basic-ladder agents (basic00_static..basic05_noise). Clean probe for levels 00-04; noise-matched probe for basic05_noise (byte-exact perceptual_noise match to its training config). Pinned each run's latest step so all 12 conditions share one checkpoint (no stale-checkpoint contamination; 360 recordings each). Generated a result-table heatmap per level and 12 qualitative videos per level (72 total, reusing sweep recordings). Wrote a fresh README for the folder + ladder6_manifest.txt.

**Headline finding**:

6 heatmaps + 72 videos generated for the still-training basic 6-level ladder; avoidance strengthens up the ladder (b00_static caught in ~17 steps, 70% time near predator; b05_noise keeps most distance, 20% time near animal). b04_jump shows a short-survival dip worth revisiting.

**What's next**:

Re-run the same pipeline ($CLAUDE_JOB_DIR/tmp/ladder6_avoidance.sh) to overwrite in place once training completes, then do the real cross-level behavioral read.

**Detailed sources**:

results/eval/avoidance/README.md, results/eval/avoidance/ladder6_manifest.txt, docs/experiments/active/behavior_measures/interoceptive_behavior_measure_study.md

## Notes

_(free-form bullets; no script involvement)_

---

*See `docs/diary/README.md` for what each section is for.*
