# Diary — 2026-09-01

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 17:00 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |
| 14:45 | (open) | 96e71c7b-dc03-44c9-a98c-1c2acc86e0d9 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 15:29 | 96e71c7b | insight | Injury and nutrition are hidden by design; the agent's felt-body channel is 2 dims (Satiation + alpha-kernel Interoceptive Nociception, peaking 3 steps after damage) | [20260901_1528_interoceptive_channel_is_two_dims_by_design](../llm_wiki/entries/env_entities/20260901_1528_interoceptive_channel_is_two_dims_by_design.md) |
| 15:29 | 96e71c7b | insight | FiLM literature verdict: contiguous grouping has no refereed support and LOSES in the one controlled RL test (GEAR); self-conditioning now has active negative evidence (HyperMARL) | [20260901_1528_film_literature_verdict_grouping_and_self_conditioning](../llm_wiki/entries/nmn_diagnosis/20260901_1528_film_literature_verdict_grouping_and_self_conditioning.md) |
| 15:29 | 96e71c7b | insight | lr_critic has zero code consumers and affects ALL rPPO not just NMN; migration must be per-file since ppo.yaml declares a critic rate HIGHER than its actor rate | [20260901_1528_lr_critic_dead_across_all_rppo_per_file_migration](../llm_wiki/entries/nmn_diagnosis/20260901_1528_lr_critic_dead_across_all_rppo_per_file_migration.md) |
| 15:29 | 96e71c7b | insight | Measured the MC bootstrap units bug: raw return sigma 23-25, so H4 delivers ~4% of its correction; ~35% of training targets under-credited in every rPPO MC run | [20260901_1528_mc_bootstrap_units_measured_sigma_25](../llm_wiki/entries/nmn_diagnosis/20260901_1528_mc_bootstrap_units_measured_sigma_25.md) |
| 15:29 | 96e71c7b | insight | CORRECTION: the homeostatic reward IS a function of injury, so the precondition for modulating the critic is met — the false claim had propagated from an ideas doc into the refactor plan | [20260901_1528_reward_is_function_of_injury_licenses_critic_mod](../llm_wiki/entries/nmn_diagnosis/20260901_1528_reward_is_function_of_injury_licenses_critic_mod.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `96e71c7b` — FiLM-in-RL literature corpus + modulation-site refactor planned; two pre-refactor bugs measured

**Full session UUID** (for `claude --resume`): `96e71c7b-dc03-44c9-a98c-1c2acc86e0d9`

**What this session did** (in plain words):

- **Earlier in session**: fixed 5 code-review findings in the rPPO trainer (GAE overeating guard, PRNG reset-key aliasing, LSTM+modulation guard, starvation-label guard, narrowed except) and launched/terminated NMN comparison runs.
- Built a new literature corpus **`docs/project/references/modulation_in_rl/`** — 17 PDFs fetched, full per-paper review of the 10 that implement FiLM-style scale-and-shift, plus two cross-corpus syntheses and two surveys (corpus-wide and 2024-2026 top-venue web).
- Planned a **modulation-site refactor**: adds actor and critic modulation at every hidden layer, makes temperature opt-in (default off), makes the RNN site config-selectable (`activation` default vs legacy `gate_bias`), and makes all four sites individually toggleable. Plan-reviewed (SOUND WITH CONCERNS -> Critical cleared).
- Found and corrected a factual error of my own that had propagated into the plan: **our reward IS a function of injury** (`calculate_drive` is an L2 distance over satiation and injury), so the published precondition for modulating the critic is met, not unmet.
- **Measured** the open Monte-Carlo bootstrap units bug rather than leaving it analytic: raw MC return sigma is 23-25 across four recorded rollouts, so the critic's normalised-scale value seeds a raw-reward accumulation at ~1/25 of correct magnitude.
- Planned two pre-refactor fixes (MC bootstrap units + wiring the dead `lr_critic` and adding `lr_modulator`), to land BEFORE the refactor so its before/after verification runs on clean code.

**Headline finding**:

The H4 window-edge bootstrap delivers about 4% of its intended correction — the critic is trained on per-window z-scored returns (sigma 23-25) but its raw output seeds an un-normalised reward accumulation, leaving ~35% of training targets materially under-credited in every rPPO MC run ever trained.

**What's next**:

1. Fix plan for the two bugs is in draft; plan-review it, then implement. Both must land before the refactor's fixture capture.
2. Then implement the modulation-site refactor (Part A) plus the modulator input-slicing (Part B, user-approved).
3. Experiment design is NOT yet specified — environment, seed count and node allocation are still open. The sensor-ladder study's own top recommendation was seed replication; do not repeat single-seed arms.
4. Open strategic question: the target behaviour (hide more when injured) is currently the OPPOSITE of what the agent does, and two interventions already failed to reverse it.

**Detailed sources**:

- Refactor plan: [`MODULATION_SITE_REFACTOR`](../develop/active/neuromodulation/MODULATION_SITE_REFACTOR.md) · review: [`plan_modulation_site_refactor`](../reviews/plan_modulation_site_refactor.md)
- Literature: [`modulation_in_rl_lit_review`](../project/references/modulation_in_rl/modulation_in_rl_lit_review.md) (10 papers) · discussion log: [`20260805_film_rl_context_dependent_policy_discussion`](../project/ideas/20260805_film_rl_context_dependent_policy_discussion.md)
- Commits: `a6aa7f9` `ebb5f6b` `8565584` `95f4943` `9394965` `c36a6355` `2e7da8b5` `55bcce54` `da654f1`

---

### Session `4efbe660` — Dose-response binning bug found and fixed; pipeline plan rev 3; artifact republished

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- A plan review of the analysis-pipeline refactor falsified one of the plan's own promises, and checking it found a real bug: `build_time_course.py` paired the nociception the agent feels at a given row with its behaviour on that **same** row, when the action producing the row was chosen while feeling the row before it. Every other sweep in the analysis uses the previous row. Fixed at source (`c20ff5db`) rather than ported bug-for-bug; all fourteen arms rebuilt.
- Rebuilt Figure 9 and re-verified its four unchanged claims against the new data; updated the numbers in the report, the artifact template, and the published page.
- Plan revision 3 closes the review's two Criticals: the driver's guarantee is split into unbypassable asserts vs auditable provisions with a divergence register, and the population contract splits across open/write/read so it no longer deadlocks after a collection top-up.
- Mirrored all fifteen axes sentences into the markdown report and made `build_artifact.py` enforce a verbatim match, refusing to write the page on drift.
- Moved the plan under `active/refactors/` so `regen_dev_index.py` stops failing; `INDEX.md` had been stale for every session since 2026-08-31.

**Headline finding**:

The felt-nociception dose-response was binned on the wrong row; correcting it moves the published range from 12.6-17.8% / 31.3-49.6% bush dwell to 12.6-17.9% / 31.7-49.5% -- a largest single change of 0.4 percentage points, so the finding survives, but the convention was wrong and nothing in the codebase would have caught it.

**What's next**:

1. `KNOWN_BUGS.md` is uncommitted and holds ANOTHER session's work (the 2026-08-25 hiding-drivers cluster and the timectrl.py latent row are not in HEAD). Needs a decision before it is lost.
2. `timectrl.py` still carries the same contemporaneous-binning bug -- now a two-file class, neither instance caught by a test. Hand-off to senior-developer.
3. Plan rev 3 is ready for a third review round, or for implementation if the user prefers to stop reviewing.

**Detailed sources**:

- Plan: [analysis_pipeline_refactor](../develop/active/refactors/analysis_pipeline_refactor.md) (revision 3)
- Report: [sensor_ladder](../experiments/active/sensor_ladder/sensor_ladder.md); artifact republished at https://claude.ai/code/artifact/3d191a81-2aec-4f97-a3e8-993568936b5b
- Visualization: [pipeline_layout](../../scripts/analysis/pipeline_layout.html), republished
- Commits: `c20ff5db` `c71ac39e` `e98f5705` `ee7ee49a` `06948eed` `c6b02f01`

## Notes
- 17:13 — Analysis-pipeline refactor PARKED (not built — no core/ package exists; plan is at rev 3 after two NOT READY reviews). Wake-up trigger recorded in the plan: a fourth store scanner, a new guard-class bug, or a published figure that can no longer be re-run. Also: timectrl.py re-marked WON'T FIX — it is deliberately frozen with its wart documented, not an open task; the old 'script not yet corrected' wording nearly caused a wrong fix. [analysis_pipeline_refactor](docs/develop/active/refactors/analysis_pipeline_refactor.md)

- 16:40 — Bug registry: recorded the sensor-ladder dose-response contemporaneous-binning bug (fixed, `c20ff5db`; dose spread +24.68 → +25.03 pp, finding survives) and flagged contemporaneous binning as a recurrence-prone class — 2nd file after `timectrl.py`, still latent. [Known Bugs](docs/develop/active/issues/KNOWN_BUGS.md)

- 16:27 — plan-reviewer round 2 on the analysis-pipeline refactor plan: NOT READY again — revision 2 fixed all of round 1 (tiered comparison criterion verified achievable empirically, ~4 orders of margin) but introduced two new Criticals: the driver's guaranteed conventions contradict the golden gate in the guard table's 'no' cells (build_time_course's dose-response bins contemporaneously, same LATENT class as timectrl.py, feeds published lad09), and the population contract as specified deadlocks regeneration after any store growth. Review: docs/reviews/plan_analysis_pipeline_refactor.md (Round 2)

- 16:05 — plan-reviewer gates the analysis-pipeline refactor plan NOT READY — the byte-identical migration gate contradicts the declarative-accumulator redesign (float accumulation order); review at docs/reviews/plan_analysis_pipeline_refactor.md

- 15:09 — MC-units + lr-wiring fix plan amended after adversarial plan review — the safety check now reads the tail (per-update clip statistics from an unsmoothed per-iteration CSV, ~1000 iterations, environment pinned to basic/04, single seed). Ready except one blocking user decision: how much slowdown in the policy's learning is acceptable while still calling this a bug fix. [plan](docs/develop/active/issues/FIX_MC_RETURN_UNITS_AND_LEARNING_RATES.md)

- 14:47 — Plan drafted (senior-developer): MC-return units fix + wiring lr_critic/lr_modulator as one change, sequenced to land before the modulation-site refactor's fixture capture — docs/develop/active/issues/FIX_MC_RETURN_UNITS_AND_LEARNING_RATES.md

- 02:47 — plan-reviewer follow-up on sensor-ladder: confirmed C4 (report S5 narrates behaviour by the physical injury the agent cannot sense; perceived-trace reconstruction verified exact against recorded obs, perceived gap peaks t=12, extra hiding t=14-16) - appended to docs/reviews/plan_sensor_ladder.md; negative-percentage question cleared (all negatives are labelled differences, bounded at 38.3 pp)


---

*See `docs/diary/README.md` for what each section is for.*
