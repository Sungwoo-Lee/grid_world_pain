# Diary — 2026-07-27

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| _(no sessions yet today)_ | | | | | |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 02:51 — 5-perspective Dreamer investigation complete (Fable fresh-eyes re-exam): implementation CLEAN (0 red, faithfulness review + free-nats/truncation follow-ups green); learning healthy-but-slow, throughput-bound (WM plateaued, behavior still climbing 48->123 survival); ~2x wall-clock recoverable (34-44% stalls from ckpt+blocking video render, un-jitted actor 436 eager dispatches/iter, Python autoreset); settings verdict INVERTED — replay ratio NOT too low (train_ratio 64 ~ paper's data-rich 32; M over-updated at 62 replays/param), M over-sized, prescribe XS @ rr 0.25 + critic bins ±20->±6 (whole task spans ~1.2 bins); task 'different in kind' (near-potential-based reward -> death is only controllable signal; rPPO gamma=0.95 = myopic surrogate, weakens the 2.6B-step difficulty datum). Synthesis + 5 docs committed. Next: PI direction call (retune vs probe-first vs speed-first vs accept).

- 02:45 — H2b snapshot analysis (experiment-analyzer, 3c11010b/experiment-analyzer): learning-quality read of the 4 live dsrl_dp1 runs (qf721efb/fwze090k/lco3j3w8/g148xn28) vs rppo_b03/b04_mc_dp1_n110 at ~22h. Verdict: healthy-but-slow, throughput-bound — WM loss plateaus by ~8M env steps while survival still rises monotonically; Dreamer 2.1-2.4x more sample-efficient per episode/env-step but rPPO collects 113-170x steps/hour and leads 1.35x at matched wall-clock. XS matches M at matched episodes despite worse WM fit → capacity/replay not binding. Runs NOT marked done (still training). Doc: docs/experiments/active/dreamer_srl_investigation/DREAMER_SRL_INVESTIGATION.md


---

*See `docs/diary/README.md` for what each section is for.*
