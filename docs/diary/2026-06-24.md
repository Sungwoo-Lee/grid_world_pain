# Diary — 2026-06-24

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 05:19 | (open) | 428cfe9b-4b3e-4d99-8065-98b46ee41016 | (auto — session-start was not called) | Auto-created on first event. |  |
| 04:57 | (open) | 3c11010b-dbfb-4d17-af58-5b86e54d6815 | (auto — session-start was not called) | Auto-created on first event. |  |
| 04:31 | (open) | 96e71c7b-dc03-44c9-a98c-1c2acc86e0d9 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 05:19 | 428cfe9b | insight | Unquoted YAML dates parse as datetime.date (not str) and broke regen_dev_index's string sort (TypeError); fix = str() coercion of the sort key; unblocks INDEX.md regen | [20260624_0518_yaml_unquoted_date_breaks_string_sort](../memory/memories/cluster_ops/20260624_0518_yaml_unquoted_date_breaks_string_sort.md) |
| 05:19 | 428cfe9b | insight | Animal spawn-exclusion from bushes is already free: resolve_overlaps_global gives every entity a unique cell, so animals never spawn on a bush (verified 0/2000 resets) -> Phase-2 needed no code and zero reset cost | [20260624_0517_bush_spawn_exclusion_free_via_overlap_resolution](../memory/memories/env_entities/20260624_0517_bush_spawn_exclusion_free_via_overlap_resolution.md) |
| 05:19 | 428cfe9b | insight | Bush blocks_animals toggle (default off): per-obstacle flag blocks ANIMAL movement into bushes (hunt+wander collision), move_agent untouched so the agent still hides -> a true refuge; byte-transparent default-off | [20260624_0516_bush_blocks_animals_movement_toggle](../memory/memories/env_entities/20260624_0516_bush_blocks_animals_movement_toggle.md) |
| 04:57 | 3c11010b/developer | implemented | Fix 1 (compile-once @jax.jit scan) + Fix 2 (constant n_grad_steps) for dreamer_srl gradient hang | [TRAIN_STEP_COMPILE_DIAGNOSIS_AND_FIX_PLAN](../develop/active/dreamer_srl_v2/TRAIN_STEP_COMPILE_DIAGNOSIS_AND_FIX_PLAN.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| 04:31 |  | 96e71c7b/training-runner | rppo_nmn_film_curric_longL4_n114 | 114:2 | running | - | rppo_nmn_film_curric_longL4_n114 | — | [basic_curriculum_continual_result](../experiments/active/basic_curriculum/basic_curriculum_continual_result.md) |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `428cfe9b` — Olfactory-ambiguity program + env-variance + bush-refuge features

**Full session UUID** (for `claude --resume`): `428cfe9b-4b3e-4d99-8065-98b46ee41016`

**What this session did** (in plain words):

- **Olfactory-ambiguity program:** designed + launched the 10-run discrimination sweep and analyzed it (discrimination weak, reactive tank-and-hide, hunger-gating refuted but "lethality masks gating"); launched the scarcity pair + linear-decay sweep; renamed `hunger_gated`->`olfactory_ambiguity` + added README sweep tables.
- **Per-episode env-variance feature:** entity counts as per-episode ranges (static-max alloc + activation masks) + whole-grid default spawn; fixed a resource-revival blocker (two-review pass); benchmarked (~7% step cost, no recompile penalty — verified rPPO immune, shared cross-session with the Dreamer work).
- **Bush "perfect refuge" feature:** added a `blocks_animals` toggle (default off) so animals can't move into bushes (agent still hides); a test-first probe showed spawn-exclusion is already free (overlap resolution -> 0/2000), so no Phase-2 build.
- Fixed a pre-existing `regen_dev_index.py` crash (unquoted-YAML-date sort bug) that blocked the dev-doc index for all sessions.

**Headline finding**:

Shipped per-episode env-variance + bush-refuge env features (both toggleable, parity-safe, ~single-digit-% speed cost, no recompile penalty); olfactory-ambiguity sweeps show discrimination stays weak and lethality masks the hunger-gate.

**What's next**:

1. Analyze the linear-decay sweep (paired vs the decay-2.0 surface) once it finishes.
2. Analyze the hypervigilance scarce-vs-abundant pair.
3. Optionally re-benchmark the trimmed env-variance default's step-SPS on a clean node (~-7% is an estimate).

**Detailed sources**:

- Insights this session: `env_entities/` (6), `hypervigilance/` (4), `cluster_ops/` (1)
- Plans: [`PER_EPISODE_ENV_VARIANCE`](../develop/active/refactors/PER_EPISODE_ENV_VARIANCE.md), [`BUSH_BLOCKS_ANIMALS`](../develop/active/refactors/BUSH_BLOCKS_ANIMALS.md)
- Commits: env-variance `90d687f`/`7fa6183`/`61e0c73`; bush `523364a`; index fix `e8a3489`

## Notes
- 04:21 — senior-developer: Diagnosed dreamer_srl train_step compile hang — the gradient-step lax.scan ships WITHOUT the outer @jax.jit the Option-L plan prescribed, so the ~76k-line train_step recompiles every iteration (hours on GPU). Measured single step = 76,005 HLO lines / 63s CPU compile; scan-under-jit compiles all 16 steps once at 1.01x size. Prioritized fix plan written (Fix 1 outer jit, Fix 2 constant n_grad_steps). docs/develop/active/dreamer_srl_v2/TRAIN_STEP_COMPILE_DIAGNOSIS_AND_FIX_PLAN.md


---

*See `docs/diary/README.md` for what each section is for.*
