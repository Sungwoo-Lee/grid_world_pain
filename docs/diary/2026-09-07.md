# Diary — 2026-09-07

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 03:20 | (open) | 96e71c7b-dc03-44c9-a98c-1c2acc86e0d9 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 03:20 | 96e71c7b/developer | implemented | B0 bush-entry measure added to context_dependence.py + pre-launch control band measured on the five unmodulated MC seeds: Delta_B0 band -0.53..+0.10 pp (first-25 window, predator far), i.e. the unmodulated agent shows NO cover-seeking increase when wounded and the 5-seed spread is ~6x the within-run CI | [NMN_INPUT_SITE_GRID](../experiments/active/nmn_input_site_grid/NMN_INPUT_SITE_GRID.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 02:55 — experiment-designer: amended the 16-cell modulator input x site grid after plan-reviewer's NOT READY verdict — headline behavioural measure replaced with a motion-based bush-entry rate whose null is measured pre-launch on the five existing unmodulated MC seeds, resolution bands raised to 15/30, H5 split into 'vs control' and 'vs extero', 'trap, unresolved' rule added, C7 replication gate added. Design size unchanged; the reviewer's 28-run staged alternative recorded for the user to rule on. docs/experiments/active/nmn_input_site_grid/NMN_INPUT_SITE_GRID.md

- 02:42 — plan-reviewer: NOT READY on the 16-cell NMN input x site grid — Part B not in tree (10/16 cells unconfigurable; a staggered launch would strand the first 6 as un-evaluable), working tree uncommitted (no SHA for provenance), B2/B3 behavioural measures do not escape the freeze-to-heal artefact. Review: docs/reviews/plan_nmn_input_site_grid.md

- 02:28 — experiment-designer: pre-registered the 16-cell neuromodulator input x site grid (what the modulator READS x what it RE-TUNES, basic/04, MC, single seed). Design doc only — configs deferred to a second pass because both config keys are still landing (site-selection Part A in implementation, input-slice Part B approved today, not yet built). Flags a pre-launch blocker: the pending MC-return-units fix would redefine return_mode MC as the MC_FIXED semantics the 25-run study measured as ~10x slower. Doc: docs/experiments/active/nmn_input_site_grid/NMN_INPUT_SITE_GRID.md


---

*See `docs/diary/README.md` for what each section is for.*
