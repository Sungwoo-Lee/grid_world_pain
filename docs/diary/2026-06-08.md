# Diary — 2026-06-08

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 15:47 | (open) | 32cc4bdf-7b29-46c8-9503-74b424ad2c53 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 16:27 | 32cc4bdf | implemented | docs/environment/01_state_and_params.md upgraded to teaching doc — verbatim source blocks + JAX primer links for select_by_class / EnvState / EnvParams | commit `d982f82` |
| 16:24 | 32cc4bdf | implemented | 02_config_schema.md teaching upgrade: verbatim source + JAX primer links for 7 functions | [02_config_schema](../environment/02_config_schema.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `32cc4bdf` — docs/environment full v2.0 re-sync (14 docs + hub)

**Full session UUID** (for `claude --resume`): `32cc4bdf-7b29-46c8-9503-74b424ad2c53`

**What this session did** (in plain words):

- Audited all 14 `docs/environment/` reference docs + the `ENVIRONMENT_SUMMARY` hub against the current v2.0 source, treating code as source of truth.
- Fanned out **14 parallel sub-agents** (one per doc + a final hub-synthesis agent); each read its mapped source file(s), rewrote the doc, and returned a structured change-list + suspected-bug list.
- Biggest driver of staleness: the **"unified animal entity" refactor** (CP1–CP6) — old separate `pred_*`/`neutral_*` arrays merged into one `animal_*` family with class + behaviour attributes. Also caught: per-episode distributional behaviour params, the new interoceptive-nociception sensor, `predator_enabled` removed, olfaction now 3 pools, dual renderers (V1 production / V2 experimental), and dreamer-srl dual-path checkpointing.
- 13 doc commits + 1 hub commit; doc 02 (config schema) was re-run after an API socket crash on the first attempt.

**Headline finding**:

All 14 environment reference docs + the hub now match the v2.0 unified-animal implementation; 9 of them had stale pred_/neutral_ naming that is now gone.

**What's next**:

1. User triages the suspected-bug list (most actionable: `overeating_death` is a no-op; `info['termination_reason']` is unreliable when a body system is disabled).
2. Optionally route the confirmed bugs to senior-developer for fix plans.

**Detailed sources**:

- Suspected-bug list: `tmp/20260608_154726_env_doc_audit_suspected_bugs.md`
- Docs: all of `docs/environment/` (01–13 + `ENVIRONMENT_SUMMARY.md`)
- Commits: `396b332`, `514bccc`, `7266023`, `ba1b760`, `5dc1957`, `77f0c8a`, `8f7242d`, `12a99db`, `262e32d`, `ea67d1d`, `3248129`, `007110f`, `aa165a9`

## Notes

_(free-form bullets; no script involvement)_

---

*See `docs/diary/README.md` for what each section is for.*
