# Diary — 2026-08-20

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 01:25 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 16:19 | 4efbe660/developer | implemented | train.py now stamps <run>/models/provenance.json at startup (git sha/branch/dirty/argv); shared src/utils/provenance.py helper reused by eval_rollout + traj collector, which records training_git_sha/dirty/started_utc in the store manifest | [provenance](../../src/utils/provenance.py) |
| 16:16 | 4efbe660/developer | implemented | Corrected stale ULP cause in code+docs: compiler FMA fusion, not vmap/batching; bound and assertions unchanged; 67 tests green | [TRAJECTORY_COLLECTION_PIPELINE](../develop/active/behavior/TRAJECTORY_COLLECTION_PIPELINE.md) |
| 16:09 | 4efbe660 | insight | Two runs of the same environment-reset code can disagree by one float32 last-bit (about 6e-08) on the sampled animal property, because the compiler is free to fuse 'mean + std x noise' into a single instruction in one place and not the other; this was first blamed on batching (vmap) and that was wrong. | [20260820_1606_reset_ulp_divergence_is_compiler_fusion](../llm_wiki/entries/env_entities/20260820_1606_reset_ulp_divergence_is_compiler_fusion.md) |
| 02:21 | 4efbe660/developer | implemented | Trajectory pipeline review round: 19 consolidated fixes (C5 invariant bug, nnx.jit retrace +15% in-block, device guard, hard-fail restore degradation, strict spec keys, float32 default); 66 tests green | [TRAJECTORY_COLLECTION_PIPELINE](../develop/active/behavior/TRAJECTORY_COLLECTION_PIPELINE.md) |
| 01:51 | 4efbe660/senior-developer | verified | Trajectory collection pipeline — all 46 tests + parity gate re-run, 4 deviations judged, fixture regeneration independently re-derived; APPROVED for Phase 1 pilot with 8 routed items | [TRAJECTORY_COLLECTION_PIPELINE](../develop/active/behavior/TRAJECTORY_COLLECTION_PIPELINE.md) |
| 01:25 | 4efbe660/developer | implemented | Trajectory collection pipeline built + verified locally: 46 tests green, parity gate 34/34; C8 float16 saving 12.8% ESCALATED (<20% threshold), 3 forced plan deviations | [TRAJECTORY_COLLECTION_PIPELINE](../develop/active/behavior/TRAJECTORY_COLLECTION_PIPELINE.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Trajectory-collection pipeline: designed, built, reviewed, pilot green

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- Designed a training-run-agnostic tool that puts a trained agent back into its OWN training world, collects ~1M episodes, and records both per-step trajectories and the realised per-episode environment draws (which live on `EnvState` and were previously written to no artifact — they are the whole independent-variable side of any factor analysis).
- Ten structured decisions taken with the user across four question rounds: own-env rollout, fixed key set with env-following array widths, sharded parquet, deterministic policy, ~1M episodes/run, final checkpoint, CPU-or-GPU at launch, shared-but-overridable seeds, collection-only scope (no analysis layer).
- `plan-reviewer` returned NOT READY on two Criticals. One was real (the config reload could rebuild the scene the trainer *discarded*, and both verification steps were circular w.r.t. it); one I diagnosed away (4 red parity tests were stale fixtures from a deliberate 2026-07-04 start-pos change, not an env regression — 30/34 passing, zero unexplained residue). Re-review: SOUND.
- `developer` implemented; three reviewers in parallel all passed. 19 consolidated fixes applied, incl. a guard asserting an invariant the plan contradicts (resource properties re-draw on regeneration) and an `nnx.jit` built per-chunk (~977 recompiles/run).
- Storage arithmetic was wrong three times, always by using CSV- or raw-denominated compression ratios. Settled by measurement: float16 saves 12.8% store-wide, below the pre-registered 20% threshold, so float32 by the rule.
- Pilot: 25k episodes in 115s (545-595 eps/s steady state), ~7.1 GB per 1M. 200 episodes replayed unbatched from their recorded seeds match the stored draws exactly, bar the known 1-ULP float column.

**Headline finding**:

The realised per-episode environment draws are now recorded and independently verified against unbatched replay — the missing independent variable that made previous training-environment comparisons uninterpretable.

**What's next**:

1. Remove the 25k pilot store before any production run — `n_episodes` is manifest-guarded, so it will refuse (loudly, at startup) to extend into a 1M population at the same path.
2. `bug-curator`: record the vmap/unbatched 1-ULP `animal_property_sampled` divergence (corroborated as compilation-level FMA fusion, not a vmap artefact) and close the model-size-flag row (verified fixed).
3. Add a training-time git SHA to `train.py` — runs record no code version, which is why identifying affected runs needed a scan of all 334 saved configs.
4. Production collection when the user calls it: ~30 min/run single-process, well under an hour for all ten across the cluster.

**Detailed sources**:

- Plan + implementation + verification reports: [`TRAJECTORY_COLLECTION_PIPELINE`](../develop/active/behavior/TRAJECTORY_COLLECTION_PIPELINE.md)
- Schema reference: [`TRAJECTORY_STORE_SCHEMA`](../environment/TRAJECTORY_STORE_SCHEMA.md)
- Adversarial review (NOT READY -> SOUND): [`plan_trajectory_collection`](../reviews/plan_trajectory_collection.md)
- Commits: `cc6353a` (stale parity fixtures), `3131574` (pipeline, 66 tests), `eff68e6` (diary)

## Notes
- 16:07 — bug-curator: Known Bugs registry maintenance — recorded the one-last-digit float divergence on environment reset (XLA multiply-add fusion, animal_property_sampled only) as **confirmed NOT a bug** with all three ruling-out evidence lines; closed the model-size CLI-flag row (L4) as verified-fixed live (--hidden-size 77 lands in the saved config); re-diagnosed and closed the four red observability-gate parity scenarios (stale 2026-05-28 fixtures predating the deliberate start-position change 84014e4; regenerated in cc6353a, gate now 34 passed). Added a convention: a red-test row must record a diagnosis or a named owner. [docs/develop/active/issues/KNOWN_BUGS.md](docs/develop/active/issues/KNOWN_BUGS.md)


---

*See `docs/diary/README.md` for what each section is for.*
