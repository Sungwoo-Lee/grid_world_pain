# Diary — 2026-07-17

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 01:29 | (open) | 3c11010b-dbfb-4d17-af58-5b86e54d6815 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 02:06 | 3c11010b/developer | implemented | Made recurrent_ppo.yaml/dreamer_srl.yaml self-contained (reversed 4a419eb enforcement); fixed Dreamer num_envs config read | commit cb5b4da |
| 01:29 | 3c11010b/developer | implemented | Split Dreamer's train config out of the shared configs/train/default.yaml into configs/train/dreamer_srl.yaml (mirrors rPPO's override layer); smoothing_episodes stays shared-only | [DREAMER_TRAIN_CONFIG_SPLIT](../develop/active/refactors/DREAMER_TRAIN_CONFIG_SPLIT.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

_(no progress reports yet today)_

## Notes
- 02:16 — fix(config): restored training.checkpoint_frequency/max_checkpoints_to_keep fallback in configs/train/default.yaml — 4a419eb regression broke DQN/DRQN/PPO config chain (get_mandatory ValueError); rPPO/Dreamer per-algo overrides still win. Commit e191426.


---

*See `docs/diary/README.md` for what each section is for.*
