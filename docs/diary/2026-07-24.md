# Diary — 2026-07-24

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 15:24 | 00:28 | 87d7d010-fa7d-466e-9113-1a7618f94d95 | dreamer train.py integration | Integrate dreamer_srl into train.py as unified entry point (thin dispatch, unified CLI, parity-gated) — analyze/investigate/plan phase | commit `5a72712` commit `c0f0a28` commit `c68b359` commit `a4f9d5a` |
| 00:54 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |
| 00:52 | (open) | 87d7d010-fa7d-466e-9113-1a7618f94d95 | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| 01:02 | 87d7d010/senior-developer | verified | Dreamer-SRL eval telemetry fix (Track C): all 9 tests green, rPPO isolation + monotone-step confirmed | [DREAMER_SRL_EVAL_TELEMETRY_FIX](../develop/active/dreamer/DREAMER_SRL_EVAL_TELEMETRY_FIX.md) |
| 00:52 | 87d7d010/developer | implemented | Dreamer-SRL eval telemetry fix (Track C): video upload on policy_step clock, monotone explicit steps on eval/stage logs, Eval/* key routing between video and stats passes | [DREAMER_SRL_EVAL_TELEMETRY_FIX](../develop/active/dreamer/DREAMER_SRL_EVAL_TELEMETRY_FIX.md) |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Eval-during-training shipped: async behavior-probe -> live WandB, verified on node-110 100M run

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

Plan (senior-developer) -> Fable-model independent review (found 4 blocking issues: unwrapped poll, resume double-log, serial-vs-parallel ambiguity, Ctrl-C drain) -> all folded into implementation. Feature: train.py dispatches an async on-node CPU subprocess (scripts/eval/probe_eval_checkpoint.py) at every Nth checkpoint that evals via eval_rollout.py --config-list (~36s serial for all 12 conds), writes all 11x12 measures to CSV + a result.json; the trainer is the sole WandB writer, polling finished results and logging Probe/* with a custom step_metric Probe/checkpoint_episode so late-async results plot at the right x. Off by default (behavior-identical when disabled). Verified end-to-end: launched rppo_b04_probeeval_128env_100M on node 110, first two checkpoints dispatched+evaluated+logged; WandB cloud API confirms the 9 focused series present, x=checkpoint_episode. Also this session: promoted the offline dwell pipeline to scripts/eval/dwell_sweep/ (self-serve run_sweep.py), parallelized its aggregation (6.2x), and added one-process-per-checkpoint eval (3.2x).

**Headline finding**:

rPPO training now runs the 12-condition behavior probe async at every checkpoint and logs the focused 9 series (bush_dwell/survival/spatial_spread x pred/none/rabbit inj0) to WandB keyed by checkpoint; verified live on the node-110 basic04 100M run (Probe/* series confirmed at checkpoints 100301, 200149).

**What's next**:

1. Watch the node-110 run: pred survival/bush-dwell should climb from ~19s/0% as it learns. 2. Enable on other rPPO runs (point --config at a *_probeeval.yaml) or wire into the variant/GAE runs. 3. Fast follow-up: extend the hook to the Dreamer trainer (already has the pattern shape).

**Detailed sources**:

docs/develop/active/behavior/PROBE_EVAL_DURING_TRAINING.md,scripts/eval/probe_eval_checkpoint.py,configs/eval_sweeps/

---

### Session `87d7d010` — Diagnosis + 3 fix tracks landed: 9 P1s + Dreamer eval telemetry

**Full session UUID** (for `claude --resume`): `87d7d010-fa7d-466e-9113-1a7618f94d95`

**What this session did** (in plain words):

- 13-unit Fable sub-agent diagnosis over env, rPPO, dreamer_srl, eval, configs: no P0, 10 live P1s, ~65 P2s (docs/reviews/diagnosis_20260723/).
- Track A (828b77e): config-layer fail-loud fixes — legacy-scene precedence at both loader sites, ValueError on unknown noise mode/modality, 05-config interoception truly clean.
- Track B (6e82fc3): eval run_tag identity, parallel-eval step-0 seeding, wandb job_type wiring, per-step eval PRNG, eval_obs_noise guard; untangled from a parallel session's in-flight --config-list feature via validated blob staging.
- Track C (39f851b): Dreamer eval telemetry on one policy_step clock — eval videos now reach WandB (were silently dropped as backward steps), step-less log sites no longer drop training rows, video/stats estimator split (Eval/video/* namespacing), fixed eval seed kept as documented design lock.
- Every track: plan doc, red->green regression tests, dual independent verification; registry kept in step by bug-curator throughout.

**Headline finding**:

Twelve of the diagnosis findings are now fixed and verified across three tracks; Dreamer training-time eval videos will appear in WandB for the first time, and the config layer fails loudly instead of silently disabling noise or swapping worlds.

**What's next**:

1. Open P1s: ModulatedGRUCell init confound (needs a re-run decision on modulated-vs-baseline comparisons), MC window-edge bootstrap units mismatch, non-rPPO iteration_episodes clearing.
2. Parallel dwell-sweep session: commit the --config-list feature (diff is now feature-only) + add tests.
3. Triage pre-existing test_unified_parity.py S1-S4 failures.
4. Remaining P2 backlog in docs/reviews/diagnosis_20260723/ (eval_stats_num_envs dead key, Dreamer checkpoint gaps, curriculum BM rebind, env placement/PRNG quirks).

**Detailed sources**:

- Review: [review_full_diagnosis_20260723](../reviews/diagnosis_20260723/review_full_diagnosis_20260723.md)
- Plans: [FIX_CONFIG_LAYER_SILENT_FAILURES_20260723](../develop/active/issues/FIX_CONFIG_LAYER_SILENT_FAILURES_20260723.md), [FIX_EVAL_LOGGING_TRACK_B_20260723](../develop/active/issues/FIX_EVAL_LOGGING_TRACK_B_20260723.md), [DREAMER_SRL_EVAL_TELEMETRY_FIX](../develop/active/dreamer/DREAMER_SRL_EVAL_TELEMETRY_FIX.md)
- Commits: `a0b3412`, `828b77e`, `6e82fc3`, `39f851b` (+ registry `ec8dcc2`, `11c4a4a`)

## Notes
- 15:40 — Plan authored: dreamer_srl -> train.py single-entry-point integration (thin dispatch, 3 proof gates) + full divergence matrix — [plan](docs/develop/active/dreamer/DREAMER_SRL_TRAIN_PY_INTEGRATION_PLAN.md), [matrix](docs/develop/active/dreamer/DREAMER_SRL_TRAIN_PY_DIVERGENCE_MATRIX.md) (87d7d010/senior-developer)

- 01:04 — bug-curator: Track C dreamer eval-telemetry fixes recorded (commit 39f851b) — closed OPEN row 'dreamer_srl eval curve mixes two estimators' (A3) plus 2 unrecorded P2s (dropped eval videos from WandB, step-less log calls dropping training rows); added OPEN reminder row for the still-dead training.eval_stats_num_envs key on the Dreamer path. See docs/develop/active/issues/KNOWN_BUGS.md [session 87d7d010/bug-curator]


---

*See `docs/diary/README.md` for what each section is for.*
