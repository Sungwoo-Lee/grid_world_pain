# Diary — 2026-09-05

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 01:37 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — Analysis-pipeline refactor shipped — one guarded scan, every number reproduced

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- Built the gate BEFORE any code moved: a golden snapshot of all 126 products, and a comparison harness with a tolerance pre-registered in the source. Negative-controlled six ways, because a gate that has never rejected anything is not a gate.
- A third plan review found a hole in that gate: the output path was hardcoded, so a port would have overwritten the live products and then compared against what it just wrote. Fixed before porting anything.
- Built `core/` (store, scan, env) and ported all three store sweeps onto one shard loop. Consolidated five helpers that existed in three copies each — after testing whether the copies were BEHAVIOURALLY different, not just textually.
- Gated every port, then deleted the two old sweeps, then re-ran the figures to confirm the deletion changed nothing downstream.
- Rewrote and republished both artifacts; the pipeline page had been documenting files that no longer exist.

**Headline finding**:

All three ported sweeps reproduce the pre-refactor pipeline exactly — 14/14 arms each, and all fifteen figures byte-identical, before AND after the originals were deleted. Zero divergence at any point.

**What's next**:

1. The honest test of whether this was worth doing: `context_dependence.py` is the modulator study's tool and the likeliest next store scanner. If it is written against `core/`, the case is proven; if someone copies an existing sweep again, it was not.
2. Two registered follow-ups that were deliberately NOT done inside the refactor: `hiding_drivers.py:214`'s contemporaneous injury binning (reproduced bug-for-bug; correcting it moves a published number, so it needs its own adjudicated change), and folding the population stamp into products rather than a sidecar.
3. `figures/` and `supplementary/` stay frozen with their own helper copies. The honest duplication count is two, not one.

**Detailed sources**:

- Plan (rev 4, three reviews): [analysis_pipeline_refactor](../develop/active/refactors/analysis_pipeline_refactor.md)
- Gate + goldens: `scripts/analysis/core/golden.py`, `results/_golden_prerefactor_20260904/`
- Artifacts: [pipeline layout](https://claude.ai/code/artifact/db00bfca-be2d-436e-bdb9-b19c894f663d) · [sensor ladder](https://claude.ai/code/artifact/3d191a81-2aec-4f97-a3e8-993568936b5b)
- New format-defect classes: F25, F26 in [artifact_format_bugs](../develop/active/meta/artifact_format_bugs.md)
- Commits: `0362a20b` `3f79a2f3` `8ba4f899` `0e2ac0f7` `ff33b0c9` `3fdfd708` `0f0d90a1` `c0909a01`

## Notes
- 17:53 — experiment-analyzer: revised the 10M return-mode comparison analysis after plan-reviewer's 3 Critical + 7 Moderate findings. Shared-trunk mechanism downgraded from 'refuted' to UNMEASURED in both directions (loss shares are not a gradient proxy); entropy collapse relabelled a hypothesis. Ran a 2,000-episode-per-seed greedy re-evaluation of the MC and GAE_NORM arms (eval only, no retraining, 20,000 episodes): GAE_NORM beats MC by +4.47 survival steps, 95% CI [+0.75, +8.19] -- the earlier 200-episode 'dead tie' was a resolution failure, and the '+8.2-step greedy bias' was a world-sampling artefact that falls to under half a step. Dilation factor restated as 6-14x DRIFTING UPWARD (not constant); the 9-12 billion-step catch-up figure is now a lower bound. Provenance checked: the 25 runs carry two git commits (MC_RAW on a descendant touching no src/ or configs/ file), and the offline eval's source env YAML matches every run's saved config on all 133 environment keys. Docs: docs/experiments/active/return_mode_cmp/return_mode_cmp_10M.md (Appendix E has the point-by-point), plus the 1M header and both pre-registration Outcome sections. No git operation attempted (.git/index.lock still held).

- 17:29 — plan-reviewer gated the 10M return-mode analysis verdict: 3 Critical findings (mechanism 'refutation' is unmeasured not refuted; GAE_NORM-vs-MC greedy 'tie' cannot resolve a 5-step effect; 'constant 10x' contradicted by own dilation table). Core delay-not-stall result stands. Review: docs/reviews/plan_return_mode_cmp_10M.md — NOT committed (index.lock held)

- 17:17 — experiment-analyzer: 10M-episode return-mode comparison (25 runs) analysed — the textbook convention is ~10x slower, not stalled; GAE_NORM leads on the training metric but ties MC under greedy eval; the shared-trunk value-dominance mechanism failed both direct tests. Runs' diary rows are in 2026-09-04.md. Doc: docs/experiments/active/return_mode_cmp/return_mode_cmp_10M.md

- 01:38 — Session 4efbe660's Sessions row in 2026-09-04.md stays marked (open): the work spanned midnight and diary_append.py's session-end has no --date flag, so it can only close a row in TODAY's file. Not hand-edited, because the script holds a flock precisely to stop parallel sessions racing on this file. Small tooling gap worth a --date argument if anyone touches that script. The session's outcome is in today's progress report.

- 01:36 — REFACTOR COMPLETE. Both artifacts republished. Pipeline-layout page rewritten for the shipped refactor (stages 2-3, closing section, stale footer); sensor-ladder reproduce-steps repointed with no figure or number changed. New format-defect class registered: F26 — a UA-default <sup> widens the line box, invisible to BOTH review methods (no rule to read, nothing overlaps). Final state: one guarded scan for all three sweeps, 15 tests, gate at core/golden.py negative-controlled 6 ways, goldens at results/_golden_prerefactor_20260904/. Honest limits recorded on the page: figures/ and supplementary/ stay frozen with their own helper copies, so the count is two, not one.

- 01:03 — REFACTOR MIGRATION STEP 5 DONE: build_arm_data.py and build_time_course.py DELETED (commit 3fdfd708); the ports on core/scan are the only implementations. Justified by measurement — 3 sweeps gate 14/14 and all 15 PNGs byte-identical — and re-verified AFTER deletion: run_all.py rebuilds all 15 figures still byte-identical. hiding_drivers.py deliberately kept (earlier study's producer; lad06 filters arms by its CSV path). Study + published page reproduce-steps repointed. Historical references in docs/reviews and KNOWN_BUGS deliberately left alone — they record what was true when written.

- 00:57 — REFACTOR: FULL GATE PASSED. All three ported sweeps reproduce the pre-refactor pipeline exactly — build_arm_data 14/14 JSON + 14/14 npz, build_time_course 14/14, hiding_drivers aggregate.npz 43/43 arrays — and all FIFTEEN figures regenerated from the ported data are byte-identical to the committed PNGs. Zero divergence at any point. Next: retire the old sweeps (keeping hiding_drivers.py, whose CSV path lad06 depends on) and update every doc that names a moved script path.

- 00:15 — REFACTOR END-TO-END GATE PASSED: figures regenerated from the PORTED sweep are BYTE-IDENTICAL to the committed ones, 14/14 PNGs (lad09 pending the time-course sweep). That is stronger than matching JSON -- it says nothing downstream noticed the swap. hiding_drivers port also gated: reproduces the live aggregate.npz exactly, 43 arrays. Goldens re-verified unclobbered after a day of porting (37/37 npz, 94/94 JSON+CSV). Added $LADDER_FIG_ROOT so the figure gate cannot overwrite the PNGs it compares against and pass by construction.

- 00:10 — REFACTOR STEPS 6-7: both remaining sweeps ported onto core/scan (commit 557f3786). All three store scanners now share one guarded loop. collect_time_course is the first real user of core/env.perceived_nociception and its reset-boundary tests. collect_hiding_drivers deliberately does NOT replace hiding_drivers.py (the earlier study's README names it as its producer) and reproduces its contemporaneous injury binning BUG-FOR-BUG, since correcting it would move a published number under cover of a refactor -- that cell is in the divergence register. Gate so far: time-course A_baseline and B_olf_only reproduce, 501 tier-1 bit-exact + 2399 tier-2 within rtol each. Remaining 12 arms + hiding_drivers aggregate still running.


---

*See `docs/diary/README.md` for what each section is for.*
