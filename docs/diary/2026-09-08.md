# Diary — 2026-09-08

> Auto-generated by `scripts/diary_append.py` (called by the `/diary` skill). Edit the Notes section freely; treat the Sessions / Events / Training runs / Progress reports sections as append-managed (the helper script handles row insertion under `flock`).

## Sessions

| Started | Ended | Session | Label | Summary | Links |
|---|---|---|---|---|---|
| 15:26 | (open) | 4efbe660-28c2-4643-b231-d3c6d2635b5a | (auto — session-start was not called) | Auto-created on first event. |  |

## Events (chronological, newest first)

| Time | Session | Type | Subject | Link |
|---|---|---|---|---|
| _(no events yet today)_ | | | | |

## Training runs

| Started | Ended | Session | Tag | Node:GPU | Status | Cell | WandB | Result | Doc |
|---|---|---|---|---|---|---|---|---|---|
| _(no training runs yet today)_ | | | | | | | | | |

## Progress reports

> Per-session high-level progress report — plain language for a reader who has NOT seen the agent-chain detail. Other sessions append their own reports below; separate entries with a horizontal rule. Each report carries the session's full UUID at the top so a reader can resume the session via `claude --resume <UUID>`.

### Session `4efbe660` — NMN grid deepened to the sensor ladder's depth; body-vs-world pattern refuted by the twin

**Full session UUID** (for `claude --resume`): `4efbe660-28c2-4643-b231-d3c6d2635b5a`

**What this session did** (in plain words):

- Generalised the sensor-ladder arm sweep with a `--manifest` flag so it can run over a population that is not the ladder, and re-gated the ladder path against the pre-refactor golden (3 arms, JSON + npz, 0 failures) to prove the change was inert before reading any grid number.
- Ran that sweep over all 21 Monte-Carlo grid runs and all 21 GAE_NORM twin runs (42 million evaluation episodes total), plus five new figures: survival and hiding, how each episode ended, hiding against distance to the nearest predator and rabbit, predator-vs-rabbit discrimination, and what hiding costs in survival.
- Collected the GAE_NORM twin's 21 million-episode trajectory stores. Node 114 failed all three of its cells instantly — its conda env has no `pyarrow` — and those were reassigned to 113/110/111.
- Put the level-effect verdict through `plan-reviewer` BEFORE writing it. It refused my wording and was right on every count; the page now carries the narrower claim the evidence supports.
- Four rounds of render review, four new format-defect classes registered (F30-F34 plus an F11 amendment). Two of the defects were introduced by my own previous fixes.

**Headline finding**:

The one exploratory pattern the MC grid offered — the world-only modulator hiding less and answering predators more strongly than the body-only one, at all five sites — does NOT survive in the GAE_NORM twin: all six measures collapse towards zero or reverse sign, and the strongest of them (predator response) comes back with one site of five agreeing.

**What's next**:

1. Publish the updated artifact once the render gate returns clean (fifth pass in flight).
2. The GAE_NORM twin's own analysis on its own terms is still a separate, unstarted job. Its aggregates are already built at `results/analysis/nmn_gaenorm_grid/ladderstyle/`, so it is a figures-and-prose job, not a compute job.
3. Node 114 still has no `pyarrow`; anyone assigning it collection work will hit the same instant failure.
4. The 2026-09-04 vs 2026-09-07 control batch offset is now a Known Bugs row but its cause is still untraced.

**Detailed sources**:

- Analysis page: [`nmn_grid_analysis_plan`](../experiments/active/nmn_input_site_grid/nmn_grid_analysis_plan.html) — section 1d is the new work
- Format-defect register: [`artifact_format_bugs`](../develop/active/meta/artifact_format_bugs.md) — F30 to F34
- Known bug: cross-batch level hazard for the five 2026-09-04 reference runs, commit `0ec9f04a`
- Commits: `3f528c1f`, `ea32fe2b`, `b247e139`, `7f5ca196`, `b4845e91`, `6d1bf94a`, `9568f1e7`

## Notes
- 16:13 — [plan-reviewer] Gated the MC-vs-GAE_NORM estimator-comparison verdict (site grid + gaenorm twin, 21+21 runs, controls 5v5): survival +4.6 steps and the modulator null hold; two Criticals on wording — "answers a nearby predator more strongly" is really the far-from-predator hiding term dropping (near-predator hiding unchanged), and "hiding/survival trade-off is a property of the estimator not the world" is range-restricted (grids span 2 pp of hiding vs the ladder's 6) and the ladder was itself trained under MC. Findings returned inline to the parent; no review file written.

- 15:54 — professor-rl: critique memo on the modulation-site grid's optimisation dynamics — time-averaged FiLM gain/offset is a gauge quantity (absorbable by the modulated layer), so the still-sliding offsets are not a convergence failure; the one real concern is rectifier dormancy, and the contextual-vs-static question is answerable now from the 50 checkpoints per run. See [[nmn_input_site_grid_optimisation_dynamics]] (docs/project/critiques/).

- 15:09 — NMN grid replication: the exploratory body-vs-world pattern (5/5 sites in the MC grid) does NOT survive in the GAE_NORM twin — all six measures collapse or reverse; predator response flips to 1/5 sites. Closed door, recorded on the analysis page. GAE twin's own analysis still a separate job.

- 14:34 — bug-curator: recorded an open interpretation hazard in [Known Bugs](docs/develop/active/issues/KNOWN_BUGS.md) — the five unmodulated reference runs trained 2026-09-04 sit at a different level from runs trained 2026-09-07, so they give run-to-run spread but must not be used as a baseline value for a later grid (already pre-registered as confound C7).

- 14:27 — NMN grid: ran the sensor-ladder arm sweep over all 21 MC-grid runs via a new --manifest flag (ladder path re-gated against the golden on 3 arms, JSON+npz, 0 failures). Five new figures g03-g07 (survival/hiding, how it ends, hiding vs distance, predator vs rabbit, price of hiding). New level-effect signal under review by plan-reviewer before it goes on the page.

- 14:27 — plan-reviewer gated the NMN site-grid ladder-style level analysis: proposed conclusion 'the modulator does change behaviour, just not state-dependently' is NOT SUPPORTED BY THE EVIDENCE SHOWN as worded — z against a 5-seed SD is heavy-tailed (|z|>2 in 14% of null cells), survival/hiding/killed/starved are one axis (r up to 0.99), group-X survival edge halves when read against the in-grid control t1none as the page's own §1b rules, and +3.6 steps sits inside the pre-registered <15-step 'no difference' band. Supportable wording: exploratory, within-grid I-vs-X paired across the five sites, unreplicated; the GAE_NORM twin grid is the cheap replication. Findings returned inline to parent.

- 13:51 — Node 114's grid_world_pain conda env has no pyarrow (113 has 25.0.1) — trajectory collection fails instantly there with ModuleNotFoundError. Its 3 GAE_NORM cells were reassigned to 109/110/111; the env gap itself is unfixed. Check before assigning 114 any collection work.

- 13:21 — CLAIMING nodes 102,109-114 for the GAE_NORM grid trajectory collection: 21 runs (16 cells + 5 controls) x 1,000,000 episodes, one pass, seed_base 1,000,000 to match the MC grid's union exactly so the cross-grid comparison is paired.


---

*See `docs/diary/README.md` for what each section is for.*
